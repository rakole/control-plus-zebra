---
phase: 01-architecture-contracts-and-fixture-proof
plan: 04
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - eslint.config.mjs
  - tests/boundaries/import-boundaries.test.ts
  - tests/boundaries/shared-naming.test.ts
  - tests/boundaries/fixtures/illegal-core-import.ts
  - tests/boundaries/fixtures/illegal-renderer-import.ts
  - tests/boundaries/fixtures/illegal-adapter-import.ts
  - README.md
autonomous: true
requirements:
  - ARCH-07
  - TEST-03
  - ARCH-04
  - ADPT-07
user_setup: []
must_haves:
  truths:
    - "ARCH-07/TEST-03: shared core and renderer cannot import adapter-private files, and adapter-private code cannot import sibling adapter internals."
    - "Shared surfaces stay harness-neutral: no shared Gemini-named types, imports, or behavior branches leak outside adapter-private code."
    - "Adapters emit evidence and diagnostics only; verification and run-audit conclusions remain shared-core responsibilities."
    - "D-13/D-16: architecture boundaries and Gemini naming guardrails are executable checks with synthetic failure fixtures, not doc-only guidance."
  artifacts:
    - path: "eslint.config.mjs"
      provides: "Boundary and naming lint configuration"
      contains: "Gemini"
    - path: "tests/boundaries/import-boundaries.test.ts"
      provides: "Import-direction enforcement suite"
      contains: "adapters"
    - path: "tests/boundaries/shared-naming.test.ts"
      provides: "Shared naming guard against Gemini leakage"
      contains: "Gemini"
    - path: "tests/boundaries/fixtures/illegal-renderer-import.ts"
      provides: "Synthetic forbidden renderer import fixture"
  key_links:
    - from: "tests/boundaries/import-boundaries.test.ts"
      to: "tests/boundaries/fixtures/illegal-core-import.ts"
      via: "negative fixture validation"
    - from: "tests/boundaries/import-boundaries.test.ts"
      to: "src/main/adapters/fake-test/index.ts"
      via: "adapter-private boundary rule"
    - from: "tests/boundaries/shared-naming.test.ts"
      to: "src/main/core"
      via: "shared naming scan"
---

<objective>
Turn the architecture rules into hard guardrails: add import-boundary enforcement, Gemini naming checks, and synthetic violation fixtures that prove the checks will fail before later phases add more code.

Purpose: prevent Phase 2+ work from accidentally coupling shared core or renderer code to adapter-private Gemini-shaped internals.
Output: ESLint/boundary config, executable boundary suites, negative fixtures, and a README note for the Phase 1 developer commands.
Locked decisions: D-13, D-14, D-15, D-16.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-architecture-contracts-and-fixture-proof/01-CONTEXT.md
@.planning/phases/01-architecture-contracts-and-fixture-proof/01-RESEARCH.md
@.planning/phases/01-architecture-contracts-and-fixture-proof/01-02-PLAN.md
@src/main/adapters/fake-test/index.ts
@src/main/core/adapter-contract/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add import-boundary enforcement for core, renderer, and adapter folders</name>
  <files>eslint.config.mjs, tests/boundaries/import-boundaries.test.ts, tests/boundaries/fixtures/illegal-core-import.ts, tests/boundaries/fixtures/illegal-renderer-import.ts, tests/boundaries/fixtures/illegal-adapter-import.ts</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-architecture-contracts-and-fixture-proof/01-CONTEXT.md, src/main/adapters/fake-test/index.ts, src/main/core/adapter-contract/index.ts</read_first>
  <action>Implement boundary checks that fail when `src/main/core/**` imports `src/main/adapters/**`, when `src/renderer/**` imports adapter-private files, or when one adapter imports a sibling adapter. Because the real renderer tree does not exist yet, add synthetic violation fixtures under `tests/boundaries/fixtures/**` and assert the boundary checker rejects them. Keep the rule generic so the future `gemini-cli` folder is covered automatically when added.</action>
  <verify>npm run test:boundaries</verify>
  <acceptance_criteria>
    - boundary tests fail on synthetic illegal core, renderer, and adapter sibling imports
    - boundary tests pass for the current legal shared-core and fake adapter structure
    - no boundary rule requires a real Gemini implementation to exist before it can protect the tree
  </acceptance_criteria>
  <done>Import-direction guardrails are executable and proven with negative fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared naming guards against Gemini leakage and conclusion fields</name>
  <files>eslint.config.mjs, tests/boundaries/shared-naming.test.ts</files>
  <read_first>.spec/additional-instructions.md, .planning/PROJECT.md, src/main/core/model/entities.ts, src/main/core/adapter-contract/session-source-adapter.ts</read_first>
  <action>Create lint and/or boundary tests that scan shared-core and future renderer-facing surfaces for forbidden shared names such as `Gemini*` and for provider-specific behavior branches that should live behind adapter metadata. Also assert that adapter-facing shared contracts do not grow final verification or run-audit conclusion fields under shared names.</action>
  <verify>npm run lint && npm run test:boundaries</verify>
  <acceptance_criteria>
    - shared naming test fails if `Gemini` appears in shared type or runtime symbol names outside adapter-private folders
    - naming/boundary checks fail if adapter-facing shared contracts add verification or run-audit conclusion fields
    - current Phase 1 tree passes the checks with the fake adapter in place
  </acceptance_criteria>
  <done>Gemini leakage and conclusion-field regressions are blocked by executable checks.</done>
</task>

<task type="auto">
  <name>Task 3: Document the Phase 1 verification commands for future contributors</name>
  <files>README.md</files>
  <read_first>README.md, .planning/ROADMAP.md, .planning/phases/01-architecture-contracts-and-fixture-proof/01-RESEARCH.md</read_first>
  <action>Replace the placeholder README with a short developer-oriented Phase 1 status note that explains the repo currently contains the architecture-contract proof milestone, lists the verification commands `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run test:boundaries`, and calls out that shared code must stay harness-neutral with no shared `Gemini*` names.</action>
  <verify>rg "npm run lint|npm run typecheck|npm run test:boundaries|Gemini" README.md</verify>
  <acceptance_criteria>
    - `README.md` names the current milestone and lists `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run test:boundaries`
    - `README.md` states that shared code must remain harness-neutral and avoid shared `Gemini*` names
  </acceptance_criteria>
  <done>The repo landing page tells contributors how to verify the Phase 1 architecture proof.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Boundary: shared core and future renderer code -> adapter-private code; data crossing: imports and symbol references
- Boundary: contributor changes -> CI/lint/boundary checks; data crossing: new files and naming choices
</trust_boundaries>
<threats>
- threat_id: T-04-01
  category: Elevation of Privilege
  component: shared/renderer import graph
  disposition: mitigate
  mitigation_plan: "Block forbidden import directions with executable boundary tests and synthetic illegal-import fixtures."
- threat_id: T-04-02
  category: Information Disclosure
  component: shared naming and provider leakage
  disposition: mitigate
  mitigation_plan: "Fail lint/boundary checks when shared code introduces Gemini-specific names or provider-specific conclusion fields."
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run test:boundaries` passes
- [ ] synthetic illegal-import fixtures fail under the boundary checker
- [ ] `README.md` lists the Phase 1 verification commands and harness-neutral naming rule
</verification>

<success_criteria>

- All tasks completed
- Import-direction guardrails cover core, renderer, and adapter sibling boundaries
- Shared naming rules reject `Gemini*` leakage outside adapter-private code
- Negative fixtures prove the guardrails actually fail on violations
- Contributors can discover the Phase 1 verification commands from `README.md`
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-contracts-and-fixture-proof/01-04-SUMMARY.md`
</output>
