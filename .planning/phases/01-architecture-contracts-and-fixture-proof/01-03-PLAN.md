---
phase: 01-architecture-contracts-and-fixture-proof
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - tests/contract/adapter-contract.test.ts
  - tests/contract/run-adapter-contract.ts
  - tests/adapters/fake-test/fake-adapter.contract.test.ts
  - tests/adapters/fake-test/fake-adapter.golden.test.ts
  - tests/adapters/fake-test/fake-adapter.truth-rules.test.ts
  - tests/fixtures/fake-test/phase1-session.normalized.json
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - ARCH-05
  - ARCH-06
user_setup: []
must_haves:
  truths:
    - "TEST-01: every bundled adapter can be run through one reusable contract suite that checks descriptor metadata, capabilities, discovery hooks, normalization output, diagnostics, and unsupported capability behavior."
    - "TEST-02: fake-test fixture normalization is locked to a checked-in golden JSON artifact so regressions show up as diffable output instead of silent behavior drift."
    - "Phase 1 proof stays fake-adapter-first and non-Gemini-shaped while remaining ready for a future gemini-cli adapter."
    - "Adapters emit evidence and diagnostics only; verification and run-audit conclusions remain shared-core responsibilities."
  artifacts:
    - path: "tests/contract/run-adapter-contract.ts"
      provides: "Reusable adapter contract test harness"
      contains: "capabilities"
    - path: "tests/adapters/fake-test/fake-adapter.contract.test.ts"
      provides: "Fake adapter contract test entrypoint"
      contains: "fake-test"
    - path: "tests/adapters/fake-test/fake-adapter.golden.test.ts"
      provides: "Golden normalization regression suite"
      contains: "phase1-session.normalized.json"
    - path: "tests/fixtures/fake-test/phase1-session.normalized.json"
      provides: "Approved normalized proof artifact"
      contains: "\"adapterId\""
  key_links:
    - from: "tests/adapters/fake-test/fake-adapter.contract.test.ts"
      to: "tests/contract/run-adapter-contract.ts"
      via: "shared contract harness"
    - from: "tests/adapters/fake-test/fake-adapter.golden.test.ts"
      to: "tests/fixtures/fake-test/phase1-session.normalized.json"
      via: "golden output comparison"
    - from: "tests/adapters/fake-test/fake-adapter.truth-rules.test.ts"
      to: "src/main/adapters/fake-test/normalize.ts"
      via: "evidence-only output assertions"
---

<objective>
Add the executable proof that the shared contract and fake adapter are correct: reusable adapter contract tests, golden normalization coverage, and truth-rule assertions that keep unsupported states and evidence-only semantics honest.

Purpose: convert the Phase 1 architecture proof from "types compile" into a regression-resistant contract suite that future adapters, including gemini-cli, can inherit.
Output: reusable contract test harness, fake adapter contract/golden/truth-rule tests, and the approved normalized fixture snapshot.
Locked decisions: D-03, D-07, D-08, D-10, D-12.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-architecture-contracts-and-fixture-proof/01-CONTEXT.md
@.planning/phases/01-architecture-contracts-and-fixture-proof/01-RESEARCH.md
@.planning/phases/01-architecture-contracts-and-fixture-proof/01-02-PLAN.md
@src/main/core/adapter-contract/index.ts
@src/main/adapters/fake-test/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the reusable adapter contract harness</name>
  <files>tests/contract/adapter-contract.test.ts, tests/contract/run-adapter-contract.ts</files>
  <read_first>.planning/REQUIREMENTS.md, src/main/core/adapter-contract/index.ts, src/main/core/model/capabilities.ts, src/main/core/diagnostics/diagnostic.ts, src/main/adapters/fake-test/index.ts</read_first>
  <action>Create a reusable test harness that any bundled adapter can run through. It must assert descriptor metadata shape, mandatory capabilities, source discovery/discovery hook behavior, normalization output shape, diagnostics handling, and unsupported capability semantics. Keep the harness adapter-agnostic so a future `gemini-cli` adapter only needs its own test entrypoint and fixture set, not a rewritten assertion layer.</action>
  <verify>npm run test -- tests/contract/adapter-contract.test.ts</verify>
  <acceptance_criteria>
    - `tests/contract/run-adapter-contract.ts` exports reusable helpers rather than fake-adapter-only logic
    - contract harness checks descriptor metadata, capabilities, diagnostics, and normalization outputs
    - unsupported capability behavior is asserted explicitly rather than inferred from `0` or empty arrays
  </acceptance_criteria>
  <done>The shared adapter contract suite exists and can be invoked by adapter-specific tests.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the fake adapter to contract and golden expectations</name>
  <files>tests/adapters/fake-test/fake-adapter.contract.test.ts, tests/adapters/fake-test/fake-adapter.golden.test.ts, tests/fixtures/fake-test/phase1-session.normalized.json</files>
  <read_first>tests/contract/run-adapter-contract.ts, src/main/adapters/fake-test/index.ts, src/main/adapters/fake-test/fixtures/phase1-session.fixture.json</read_first>
  <action>Write the fake adapter's contract test entrypoint so it runs through the shared harness, then add a golden test that normalizes the representative fixture and compares the result against a checked-in JSON artifact. The golden JSON must be intentionally stable, readable, and scrubbed to the deterministic fields required for regression review.</action>
  <verify>npm run test -- tests/adapters/fake-test/fake-adapter.contract.test.ts tests/adapters/fake-test/fake-adapter.golden.test.ts</verify>
  <acceptance_criteria>
    - fake adapter contract test exits 0 through the shared harness
    - `tests/fixtures/fake-test/phase1-session.normalized.json` exists and includes `adapterId`
    - golden test fails when normalized output drifts from the checked-in artifact
  </acceptance_criteria>
  <done>Fake adapter behavior is locked by both contract assertions and a diffable golden artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Add truth-rule regression tests for unsupported states and evidence-only semantics</name>
  <files>tests/adapters/fake-test/fake-adapter.truth-rules.test.ts</files>
  <read_first>.spec/additional-instructions.md, .planning/PROJECT.md, src/main/adapters/fake-test/normalize.ts, src/main/core/model/capabilities.ts</read_first>
  <action>Add targeted tests that fail if fake adapter outputs flatten unsupported capabilities into numeric zero or clean status, or if adapter output grows verification-pass/fail, run-audit, or attention-reason conclusion fields. These tests should assert the Phase 1 truth rules directly so later adapter work cannot accidentally erode them.</action>
  <verify>npm run test -- tests/adapters/fake-test/fake-adapter.truth-rules.test.ts</verify>
  <acceptance_criteria>
    - unsupported capability cases are asserted as explicit unsupported/unknown states
    - adapter output tests fail if final verification or run-audit conclusions appear
    - truth-rule suite exits 0 together with the contract/golden suites
  </acceptance_criteria>
  <done>The shared truth rules are executable and regression-proof for the fake adapter path.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Boundary: normalized adapter output -> test harness and golden artifact; data crossing: deterministic JSON proof data
- Boundary: capability semantics -> verification assertions; data crossing: unsupported/unknown truth states
</trust_boundaries>
<threats>
- threat_id: T-03-01
  category: Repudiation
  component: normalization regressions
  disposition: mitigate
  mitigation_plan: "Use checked-in golden output plus reusable contract assertions so output drift is visible and reviewable."
- threat_id: T-03-02
  category: Tampering
  component: truth-rule semantics
  disposition: mitigate
  mitigation_plan: "Add dedicated tests that fail when unsupported capabilities become zero/clean or when adapter outputs grow conclusion fields."
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- tests/contract tests/adapters/fake-test` passes
- [ ] `tests/fixtures/fake-test/phase1-session.normalized.json` exists and is used by the golden test
- [ ] truth-rule tests fail if unsupported states or evidence-only semantics regress
</verification>

<success_criteria>

- All tasks completed
- Shared contract suite is reusable by future adapters
- Fake adapter fixture output is locked by a checked-in golden artifact
- Unsupported capability and evidence-only truth rules are executable tests, not prose
- Full adapter-contract test slice passes in Vitest
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-contracts-and-fixture-proof/01-03-SUMMARY.md`
</output>
