---
phase: 01-architecture-contracts-and-fixture-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vitest.config.ts
  - src/main/core/model/capabilities.ts
  - src/main/core/model/confidence.ts
  - src/main/core/model/entities.ts
  - src/main/core/model/identifiers.ts
  - src/main/core/model/index.ts
  - src/main/core/diagnostics/diagnostic.ts
  - src/main/core/diagnostics/index.ts
  - src/main/core/adapter-contract/session-source-adapter.ts
  - src/main/core/adapter-contract/types.ts
  - src/main/core/adapter-contract/index.ts
autonomous: true
requirements:
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-05
  - ARCH-06
  - ADPT-07
user_setup: []
must_haves:
  truths:
    - "D-01/D-05: shared core exports the real Phase 1 nouns - project, session, session event, session message, tool call, shell command evidence, output artifact, diagnostics, capabilities, IDs, and confidence - without shared Gemini names."
    - "Shared surfaces stay harness-neutral: no shared Gemini-named types, imports, or behavior branches leak outside adapter-private code."
    - "Adapters emit evidence and diagnostics only; verification and run-audit conclusions remain shared-core responsibilities."
    - "D-06: every normalized entity sourced from harness data includes adapterId and, where relevant, sourceId in its contract shape."
  artifacts:
    - path: "package.json"
      provides: "Phase 1 workspace scripts and dependency baseline"
      contains: "\"typecheck\""
    - path: "src/main/core/model/entities.ts"
      provides: "Harness-neutral normalized entity types"
      contains: "ShellCommandEvidence"
    - path: "src/main/core/adapter-contract/session-source-adapter.ts"
      provides: "Async-friendly adapter lifecycle contract"
      exports: ["SessionSourceAdapter"]
    - path: "src/main/core/diagnostics/diagnostic.ts"
      provides: "First-class diagnostics contract"
      contains: "Diagnostic"
  key_links:
    - from: "src/main/core/model/entities.ts"
      to: "src/main/core/model/identifiers.ts"
      via: "entity identity fields"
      pattern: "adapterId|sourceId"
    - from: "src/main/core/adapter-contract/session-source-adapter.ts"
      to: "src/main/core/model/entities.ts"
      via: "normalization output types"
    - from: "src/main/core/adapter-contract/session-source-adapter.ts"
      to: "src/main/core/diagnostics/diagnostic.ts"
      via: "diagnostic reporting"
---

<objective>
Create the Phase 1 shared-core foundation: bootstrap the TypeScript/Vitest workspace, define the harness-neutral model and diagnostics surfaces, and publish the adapter contract lifecycle that later fake and Gemini adapters will implement.

Purpose: give every later Phase 1 plan one stable, future-shaped shared contract surface instead of letting the first adapter define the architecture.
Output: package/tooling foundation plus typed shared-core files under `src/main/core/model/**`, `src/main/core/diagnostics/**`, and `src/main/core/adapter-contract/**`.
Locked decisions: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-architecture-contracts-and-fixture-proof/01-CONTEXT.md
@.planning/phases/01-architecture-contracts-and-fixture-proof/01-RESEARCH.md
@.spec/spec-from-5.5-revision-1.md
@.spec/additional-instructions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Phase 1 TypeScript and test workspace</name>
  <files>package.json, package-lock.json, tsconfig.json, vitest.config.ts</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/research/STACK.md, .planning/phases/01-architecture-contracts-and-fixture-proof/01-RESEARCH.md</read_first>
  <action>Initialize the repo as an npm workspace for shared-core Phase 1 work only. Add scripts named `typecheck`, `test`, `test:boundaries`, and `lint`. Install TypeScript 6, Vitest 4, Zod 4, and ESLint 10. Configure `tsconfig.json` to compile `src/**` and `tests/**` with Node-oriented settings, and configure `vitest.config.ts` for Node runtime tests. Do NOT scaffold Electron Forge, preload, renderer, React, or packaging files in this plan; Phase 2 owns the desktop shell.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `package.json` contains scripts for `typecheck`, `test`, `test:boundaries`, and `lint`
    - `package.json` does not contain Electron Forge, React, or renderer-only runtime dependencies
    - `tsconfig.json` includes `src/**/*` and `tests/**/*`
    - `vitest.config.ts` exists and targets a Node test environment
  </acceptance_criteria>
  <done>Workspace scripts and TypeScript/Vitest baseline exist and `npm run typecheck` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Define the harness-neutral normalized model and diagnostics surfaces</name>
  <files>src/main/core/model/capabilities.ts, src/main/core/model/confidence.ts, src/main/core/model/entities.ts, src/main/core/model/identifiers.ts, src/main/core/model/index.ts, src/main/core/diagnostics/diagnostic.ts, src/main/core/diagnostics/index.ts</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/01-architecture-contracts-and-fixture-proof/01-CONTEXT.md, .spec/spec-from-5.5-revision-1.md</read_first>
  <action>Create the shared normalized model modules for `HarnessId`, `SourceId`, deterministic native-derived IDs, confidence values, structured capabilities, diagnostics, and the real Phase 1 nouns: `Project`, `Session`, `SessionEvent`, `SessionMessage`, `ToolCall`, `OutputArtifact`, `ShellCommandEvidence`, and related evidence records. Ensure every normalized entity created from harness data includes `adapterId` and includes `sourceId` wherever the source relationship exists. Keep verification-result, run-audit-status, and attention-reason concepts out of these shared exports.</action>
  <verify>npm run typecheck && rg "interface Gemini|type Gemini|class Gemini" src/main/core/model src/main/core/diagnostics</verify>
  <acceptance_criteria>
    - `src/main/core/model/entities.ts` defines `SessionEvent`, `SessionMessage`, `ToolCall`, `OutputArtifact`, and `ShellCommandEvidence`
    - normalized entity contracts contain `adapterId`
    - source-derived entities contain `sourceId`
    - `rg "interface Gemini|type Gemini|class Gemini" src/main/core/model src/main/core/diagnostics` returns no matches
  </acceptance_criteria>
  <done>Shared model and diagnostics types compile, expose the locked nouns, and remain harness-neutral.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the adapter lifecycle contract with evidence-only outputs</name>
  <files>src/main/core/adapter-contract/session-source-adapter.ts, src/main/core/adapter-contract/types.ts, src/main/core/adapter-contract/index.ts</files>
  <read_first>.planning/phases/01-architecture-contracts-and-fixture-proof/01-CONTEXT.md, .planning/phases/01-architecture-contracts-and-fixture-proof/01-RESEARCH.md, src/main/core/model/entities.ts, src/main/core/model/capabilities.ts, src/main/core/diagnostics/diagnostic.ts</read_first>
  <action>Define `HarnessDescriptor`, `SessionSourceAdapter`, source/artifact refs, and normalization result types with the explicit lifecycle seams `validateSourceRoot`, `discoverSources`, `discoverArtifacts`, `parseArtifact`, `normalize`, and optional artifact loading. Make the APIs async-friendly from day one. Shape normalization output so adapters can emit projects, sessions, events, messages, tool calls, shell evidence, output artifacts, and diagnostics, but cannot set final verification states, run-audit classifications, or attention reasons.</action>
  <verify>npm run typecheck && rg "validateSourceRoot|discoverSources|discoverArtifacts|parseArtifact|normalize" src/main/core/adapter-contract/session-source-adapter.ts</verify>
  <acceptance_criteria>
    - `SessionSourceAdapter` exports all five locked lifecycle methods
    - contract result types reference shared model and diagnostics modules rather than adapter-private types
    - adapter contract files do not define verification-state or run-audit-status enums
    - `src/main/core/adapter-contract/index.ts` re-exports the contract surface needed by adapters and the registry
  </acceptance_criteria>
  <done>The shared adapter contract is future-shaped, evidence-only, and compiles cleanly against the shared model.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Boundary: local planning docs and requirements -> shared-core contract files; data crossing: architecture rules and requirement IDs
- Boundary: future adapter-private raw events -> shared normalized model; data crossing: evidence, diagnostics, and identity fields
</trust_boundaries>
<threats>
- threat_id: T-01-01
  category: Tampering
  component: adapter-contract outputs
  disposition: mitigate
  mitigation_plan: "Keep normalization outputs evidence-only and validate them against shared model and diagnostics contracts."
- threat_id: T-01-02
  category: Spoofing
  component: normalized identifiers
  disposition: mitigate
  mitigation_plan: "Derive IDs from adapter identity, source identity, and native identity so later adapters cannot collide or impersonate each other."
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `rg "interface Gemini|type Gemini|class Gemini" src/main/core/model src/main/core/diagnostics` returns no matches
- [ ] `src/main/core/adapter-contract/session-source-adapter.ts` contains `validateSourceRoot`, `discoverSources`, `discoverArtifacts`, `parseArtifact`, and `normalize`
</verification>

<success_criteria>

- All tasks completed
- Shared-core workspace exists without Electron shell files
- Shared model exports the locked harness-neutral nouns and identity fields
- Adapter contract lifecycle is async-friendly and evidence-only
- No shared `Gemini*` type names were introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-contracts-and-fixture-proof/01-01-SUMMARY.md`
</output>
