---
phase: 01-architecture-contracts-and-fixture-proof
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/main/core/registry/adapter-registry.ts
  - src/main/core/registry/index.ts
  - src/main/core/registry/register-bundled-adapters.ts
  - src/main/adapters/fake-test/descriptor.ts
  - src/main/adapters/fake-test/discovery.ts
  - src/main/adapters/fake-test/index.ts
  - src/main/adapters/fake-test/normalize.ts
  - src/main/adapters/fake-test/parse.ts
  - src/main/adapters/fake-test/types.ts
  - src/main/adapters/fake-test/fixtures/phase1-session.fixture.json
  - tests/adapters/fake-test/fake-adapter.smoke.test.ts
autonomous: true
requirements:
  - ARCH-01
  - ADPT-01
  - ADPT-02
  - ADPT-07
user_setup: []
must_haves:
  truths:
    - "ARCH-01/ADPT-01: a future adapter can be added by implementing the shared adapter contract and registering a descriptor without editing fake adapter internals."
    - "ADPT-02: fake-test can discover one non-Gemini fixture and normalize it into shared projects, sessions, events, messages, tool calls, shell evidence, output artifacts, diagnostics, and capabilities."
    - "Shared surfaces stay harness-neutral: no shared Gemini-named types, imports, or behavior branches leak outside adapter-private code."
    - "Adapters emit evidence and diagnostics only; verification and run-audit conclusions remain shared-core responsibilities."
    - "D-11/D-12: the fake raw fixture is intentionally non-Gemini-shaped and produces a stable end-to-end proof artifact."
  artifacts:
    - path: "src/main/adapters/fake-test/index.ts"
      provides: "Fake adapter entrypoint"
      exports: ["fakeTestAdapter"]
    - path: "src/main/adapters/fake-test/fixtures/phase1-session.fixture.json"
      provides: "Representative non-Gemini raw harness fixture"
      contains: "\"sessionId\""
    - path: "src/main/core/registry/register-bundled-adapters.ts"
      provides: "Bundled adapter registration wiring"
      contains: "fake-test"
    - path: "tests/adapters/fake-test/fake-adapter.smoke.test.ts"
      provides: "Single-fixture smoke proof through the registry"
      contains: "normalize"
  key_links:
    - from: "src/main/adapters/fake-test/index.ts"
      to: "src/main/core/adapter-contract/session-source-adapter.ts"
      via: "adapter implementation"
    - from: "src/main/core/registry/register-bundled-adapters.ts"
      to: "src/main/adapters/fake-test/index.ts"
      via: "composition-root registration"
    - from: "src/main/adapters/fake-test/normalize.ts"
      to: "src/main/core/model/entities.ts"
      via: "normalized output mapping"
      pattern: "adapterId|sourceId"
---

<objective>
Implement the fake adapter proof path: create the adapter descriptor and fixture, map fake raw data through parse/normalize lifecycle seams, and wire the bundled registry so the fake adapter proves the shared contract end to end.

Purpose: demonstrate that the first working adapter is not Gemini-hardcoded and that the registry/composition root can host adapters without shared-core importing adapter internals.
Output: fake adapter modules, one representative fixture, registry wiring, and a smoke test that runs the fixture through the adapter path once.
Locked decisions: D-09, D-10, D-11, D-12, D-15.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-architecture-contracts-and-fixture-proof/01-CONTEXT.md
@.planning/phases/01-architecture-contracts-and-fixture-proof/01-RESEARCH.md
@.planning/phases/01-architecture-contracts-and-fixture-proof/01-01-PLAN.md
@.spec/spec-from-5.5-revision-1.md
@.spec/additional-instructions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the fake adapter descriptor, raw fixture, and discovery surfaces</name>
  <files>src/main/adapters/fake-test/descriptor.ts, src/main/adapters/fake-test/discovery.ts, src/main/adapters/fake-test/types.ts, src/main/adapters/fake-test/fixtures/phase1-session.fixture.json</files>
  <read_first>.planning/phases/01-architecture-contracts-and-fixture-proof/01-CONTEXT.md, .planning/phases/01-architecture-contracts-and-fixture-proof/01-RESEARCH.md, src/main/core/adapter-contract/session-source-adapter.ts, src/main/core/model/capabilities.ts</read_first>
  <action>Create the `fake-test` harness descriptor with mandatory structured capabilities and a simple fake raw event schema that is visibly different from Gemini CLI artifacts. Add discovery helpers that treat the fixture file as one source and one raw artifact under the adapter contract. Include platform metadata, display metadata, and default-root hints, but keep the raw fixture small and deterministic.</action>
  <verify>npm run typecheck && test -f src/main/adapters/fake-test/fixtures/phase1-session.fixture.json</verify>
  <acceptance_criteria>
    - `src/main/adapters/fake-test/descriptor.ts` exports adapter metadata with id `fake-test`
    - `src/main/adapters/fake-test/fixtures/phase1-session.fixture.json` exists and is not shaped like Gemini `.project_root` or chat JSONL artifacts
    - fake adapter capabilities are explicit rather than omitted or defaulted
  </acceptance_criteria>
  <done>The fake adapter has a real descriptor and a stable non-Gemini fixture source.</done>
</task>

<task type="auto">
  <name>Task 2: Implement parse and normalize flow with deterministic IDs</name>
  <files>src/main/adapters/fake-test/parse.ts, src/main/adapters/fake-test/normalize.ts, src/main/adapters/fake-test/index.ts</files>
  <read_first>src/main/core/model/entities.ts, src/main/core/model/identifiers.ts, src/main/core/diagnostics/diagnostic.ts, src/main/core/adapter-contract/session-source-adapter.ts, src/main/adapters/fake-test/types.ts</read_first>
  <action>Implement the fake adapter lifecycle methods so the fixture is parsed into adapter-private raw events and then normalized into shared projects, sessions, events, messages, tool calls, shell evidence, output artifact refs, diagnostics, and capabilities. Derive IDs from adapter identity, source identity, and native fixture identity. Keep all verification-state and run-audit interpretation out of the adapter output.</action>
  <verify>npm run typecheck && rg "adapterId|sourceId" src/main/adapters/fake-test/normalize.ts</verify>
  <acceptance_criteria>
    - normalization output includes project, session, event/message, and evidence records
    - normalized records include `adapterId` and include `sourceId` where the source relationship exists
    - fake adapter output does not define verification-pass/fail or run-audit-clean/needs-review fields
  </acceptance_criteria>
  <done>The fake adapter can normalize its fixture into shared proof objects with deterministic IDs and explicit diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Register the fake adapter and add one end-to-end smoke proof</name>
  <files>src/main/core/registry/adapter-registry.ts, src/main/core/registry/index.ts, src/main/core/registry/register-bundled-adapters.ts, tests/adapters/fake-test/fake-adapter.smoke.test.ts</files>
  <read_first>src/main/adapters/fake-test/index.ts, src/main/core/adapter-contract/index.ts, .spec/spec-from-5.5-revision-1.md</read_first>
  <action>Create the composition-root registry surface that imports bundled adapter entrypoints and exposes descriptor listing plus adapter lookup by id. Register `fake-test` there without introducing any adapter-private import into shared model or diagnostics modules. Add a smoke test that loads the fake fixture through the fake adapter path once and asserts at least one normalized project, session, event/message, and diagnostic/capability object come back.</action>
  <verify>npm run test -- tests/adapters/fake-test/fake-adapter.smoke.test.ts</verify>
  <acceptance_criteria>
    - registry files contain `fake-test` registration through the composition root only
    - shared-core model and diagnostics modules still have no imports from `src/main/adapters/**`
    - `tests/adapters/fake-test/fake-adapter.smoke.test.ts` exits 0 and proves one fixture can normalize end to end
  </acceptance_criteria>
  <done>Fake adapter proof path is live through the registry and backed by a smoke test.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Boundary: fake raw fixture -> adapter-private raw events; data crossing: untrusted local fixture contents
- Boundary: adapter-private normalization -> shared registry and shared model; data crossing: normalized evidence and diagnostics
</trust_boundaries>
<threats>
- threat_id: T-02-01
  category: Tampering
  component: fake fixture parsing
  disposition: mitigate
  mitigation_plan: "Keep fixture schema explicit and emit diagnostics on malformed or incomplete data instead of silently coercing it."
- threat_id: T-02-02
  category: Spoofing
  component: bundled adapter registry
  disposition: mitigate
  mitigation_plan: "Register adapters by explicit descriptor id and derive normalized IDs from adapter/source/native identity."
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm run test -- tests/adapters/fake-test/fake-adapter.smoke.test.ts` passes
- [ ] `src/main/core/registry/register-bundled-adapters.ts` registers `fake-test`
- [ ] fake normalized outputs include `adapterId` and `sourceId` fields where relevant
</verification>

<success_criteria>

- All tasks completed
- `fake-test` is a real bundled adapter, not a stub comment or fixture-only folder
- One representative non-Gemini fixture normalizes through the shared contract path
- Registry wiring proves adapters plug in through composition root imports only
- Adapter outputs remain evidence-only and diagnostics-first
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-contracts-and-fixture-proof/01-02-SUMMARY.md`
</output>
