---
phase: 06-harness-neutral-triage-ui
plan: 05
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - tests/main/ipc/triage-view-model-service.test.ts
  - tests/main/ipc/session-detail-view-model-service.test.ts
  - tests/main/ipc/run-audit-view-model-service.test.ts
  - tests/main/ipc/diagnostics-view-model-service.test.ts
  - tests/renderer/overview-route.test.tsx
  - tests/renderer/projects-route.test.tsx
  - tests/renderer/sessions-route.test.tsx
  - tests/renderer/session-detail-route.test.tsx
  - tests/renderer/run-audit-route.test.tsx
  - tests/renderer/diagnostics-route.test.tsx
  - tests/renderer/triage-truth-states.test.tsx
  - tests/boundaries/import-boundaries.test.ts
  - tests/boundaries/shared-naming.test.ts
  - src/main/ipc/view-models.ts
  - src/renderer/components/triage/TruthStateBadge.tsx
  - src/renderer/routes/OverviewRoute.tsx
  - src/renderer/routes/ProjectsRoute.tsx
  - src/renderer/routes/SessionsRoute.tsx
  - src/renderer/routes/SessionDetailRoute.tsx
  - src/renderer/routes/RunAuditRoute.tsx
  - src/renderer/routes/DiagnosticsRoute.tsx
autonomous: true
requirements:
  - TEST-07
  - UI-08
  - UI-09
must_haves:
  truths:
    - "The final Phase 6 regression suite proves unsupported and unknown states never render as zero, `Passed`, or `Clean` across Overview, Projects, Sessions, Session Detail, Run Audit, and Diagnostics."
    - "Boundary and shared-naming tests continue to reject adapter-private imports and shared provider-specific logic beyond display labels or capability metadata."
    - "Node-side view-model tests and renderer route tests cover both fake and Gemini-backed truths where relevant so the UI is not accidentally hardcoded to one harness."
    - "Any production-code changes in this plan are only the smallest truth-state or adapter-neutral fixes discovered while adding the regression suite."
  artifacts:
    - path: "tests/renderer/triage-truth-states.test.tsx"
      provides: "Cross-route truth-state matrix for unsupported, unknown, not-run, placeholder, and no-evidence rendering"
      contains: "Unsupported"
    - path: "tests/boundaries/shared-naming.test.ts"
      provides: "Guardrail against shared provider-specific route or DTO behavior"
    - path: "tests/main/ipc/triage-view-model-service.test.ts"
      provides: "Node-side overview/projects/sessions truth-state coverage"
  key_links:
    - from: "tests/renderer/triage-truth-states.test.tsx"
      to: "src/renderer/components/triage/TruthStateBadge.tsx"
      via: "shared truth-state rendering assertions across routes"
      pattern: "Unknown|Unsupported|Not Run"
    - from: "tests/boundaries/shared-naming.test.ts"
      to: "src/main/ipc/view-models.ts"
      via: "shared DTO naming and provider-neutral field checks"
      pattern: "Gemini"
    - from: "tests/main/ipc/*.test.ts"
      to: "src/main/core/cache/file-backed-cache-store.ts"
      via: "fake and Gemini-backed truth-state projection"
      pattern: "derived.sessions"
---

<objective>
Lock Phase 6 with focused service, renderer, and boundary regressions that prove capability gates and adapter-neutral rendering across every new triage page.

Purpose: Phase 6 is only successful if the UI stays truthful under missing evidence, unsupported capabilities, and multiple harnesses. This plan hardens those guarantees before verification and closeout.
Output: cross-route truth-state tests, expanded node and renderer regression coverage, boundary protections against provider-specific shared logic, and any small fixes required to make the final suite green.
</objective>

<execution_context>
@/Users/rhishi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rhishi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-harness-neutral-triage-ui/06-CONTEXT.md
@.planning/phases/06-harness-neutral-triage-ui/06-RESEARCH.md
@.planning/phases/06-harness-neutral-triage-ui/06-UI-SPEC.md
@.planning/phases/06-harness-neutral-triage-ui/06-PATTERNS.md
@.planning/phases/06-harness-neutral-triage-ui/06-VALIDATION.md
@.planning/phases/06-harness-neutral-triage-ui/06-01-PLAN.md
@.planning/phases/06-harness-neutral-triage-ui/06-02-PLAN.md
@.planning/phases/06-harness-neutral-triage-ui/06-03-PLAN.md
@.planning/phases/06-harness-neutral-triage-ui/06-04-PLAN.md
@tests/main/ipc/session-view-model-service.test.ts
@tests/renderer/{sessions-route.test.tsx,data-sources-route.test.tsx}
@tests/boundaries/{import-boundaries.test.ts,shared-naming.test.ts}
@src/main/ipc/view-models.ts
@src/renderer/components/triage/TruthStateBadge.tsx

<interfaces>
Existing seams to preserve:
- Shared code may display provider names through descriptor metadata, but shared DTO names, renderer route behavior, and truth-state logic must stay harness-neutral.
- Focus fixes only on test-discovered truth-state or boundary gaps. Do not reopen route architecture, add new features, or widen preload surface area here.
- Final verification should be able to rely on the focused route and service tests added in prior plans plus this cross-route truth-state suite.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add final Phase 6 truth-state and adapter-neutral regression coverage</name>
  <files>tests/main/ipc/triage-view-model-service.test.ts, tests/main/ipc/session-detail-view-model-service.test.ts, tests/main/ipc/run-audit-view-model-service.test.ts, tests/main/ipc/diagnostics-view-model-service.test.ts, tests/renderer/overview-route.test.tsx, tests/renderer/projects-route.test.tsx, tests/renderer/sessions-route.test.tsx, tests/renderer/session-detail-route.test.tsx, tests/renderer/run-audit-route.test.tsx, tests/renderer/diagnostics-route.test.tsx, tests/renderer/triage-truth-states.test.tsx, tests/boundaries/import-boundaries.test.ts, tests/boundaries/shared-naming.test.ts, src/main/ipc/view-models.ts, src/renderer/components/triage/TruthStateBadge.tsx, src/renderer/routes/OverviewRoute.tsx, src/renderer/routes/ProjectsRoute.tsx, src/renderer/routes/SessionsRoute.tsx, src/renderer/routes/SessionDetailRoute.tsx, src/renderer/routes/RunAuditRoute.tsx, src/renderer/routes/DiagnosticsRoute.tsx</files>
  <read_first>06-CONTEXT decisions D-05 through D-18; 06-UI-SPEC State Vocabulary, Interaction Contract, and Copywriting Contract; 06-VALIDATION.md full file; all prior 06-0x plans; tests/boundaries/shared-naming.test.ts; tests/renderer/sessions-route.test.tsx; tests/main/ipc/session-view-model-service.test.ts</read_first>
  <behavior>
    - Test 1: a new `tests/renderer/triage-truth-states.test.tsx` covers unsupported, unknown, not-run, no-evidence, and Phase 7 placeholder states across Overview, Projects, Sessions, Session Detail, Run Audit, and Diagnostics.
    - Test 2: node-side service tests cover both fake and Gemini-backed data where relevant so project rollups, session detail, audit groups, and diagnostics stay adapter-neutral and do not hardcode one harness shape.
    - Test 3: boundary tests still fail if shared renderer or IPC code imports adapter-private files or introduces shared `Gemini*` DTO or route logic.
    - Test 4: renderer tests assert forbidden regressions explicitly, including hidden sections for unsupported states and accidental `0`, `Passed`, or `Clean` fallbacks when evidence is missing.
    - Test 5: any production-code touchups in this plan are minimal and directly tied to the new regression expectations rather than new feature scope.
  </behavior>
  <action>Add `tests/renderer/triage-truth-states.test.tsx` as the cross-route matrix for unsupported, unknown, not-run, and placeholder rendering. Expand the per-surface node and renderer tests from earlier plans so fake and Gemini-backed fixtures both exercise adapter-neutral data flows where feasible, especially around harness filters, grouped audit evidence, diagnostics, and capability warnings. Extend `tests/boundaries/shared-naming.test.ts` and `tests/boundaries/import-boundaries.test.ts` if needed so shared renderer and IPC code cannot drift into provider-specific naming or adapter-private imports while Phase 6 surfaces grow. If these tests expose small gaps in `src/main/ipc/view-models.ts`, `TruthStateBadge.tsx`, or the new triage routes, apply only the narrowest fixes required to keep labels, placeholders, and route behavior aligned with the approved Phase 6 contracts. Do not add new features, new routes, generic preload APIs, or Phase 7 git or GitHub logic in this plan.</action>
  <verify>
    <automated>npm run test -- --project node tests/main/ipc</automated>
    <automated>npm run test -- --project renderer tests/renderer</automated>
    <automated>npm run test:boundaries</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>`tests/renderer/triage-truth-states.test.tsx` exists and fails on unsupported or unknown state regressions across every Phase 6 surface; node-side triage services remain adapter-neutral under fake and Gemini-backed data; boundary tests still reject provider-specific shared logic and adapter-private imports; and the focused node, renderer, boundary, typecheck, and lint commands all pass together.</acceptance_criteria>
  <done>Phase 6 closes with durable truth-state and adapter-neutral regressions instead of relying on spot checks or one-harness behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route and service regressions -> user trust | Hidden capability gaps or provider-specific UI drift can silently undermine the product's core value. |
| new tests -> production code touchups | Final hardening should fix only discovered regressions, not reopen scope or architecture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Spoofing | truth-state rendering | mitigate | Add explicit cross-route assertions that unsupported, unknown, and placeholder states never render as success or zero. |
| T-06-05-02 | Tampering | shared provider-neutral boundaries | mitigate | Keep boundary tests failing on shared `Gemini*` logic and adapter-private imports as new routes and DTOs land. |
| T-06-05-03 | Repudiation | final Phase 6 verification | mitigate | Run the focused node, renderer, boundary, typecheck, and lint suite together so each regression points to the exact surface that drifted. |
| T-06-05-SC | Tampering | late feature creep | mitigate | Allow only the smallest production-code fixes required by the regression suite; no new features or Phase 7 logic in this hardening plan. |
</threat_model>

<verification>
Plan-level checks:
- `npm run test -- --project node tests/main/ipc`
- `npm run test -- --project renderer tests/renderer`
- `npm run test:boundaries`
- `npm run typecheck`
- `npm run lint`
</verification>

<success_criteria>
- Cross-route truth-state regressions exist for every Phase 6 surface.
- Boundary tests still protect adapter-neutral shared code.
- The final focused node, renderer, boundary, typecheck, and lint suite passes together before verification.
</success_criteria>

<source_coverage_audit>
GOAL: prove Phase 6 stays truthful and adapter-neutral under missing evidence and multiple harnesses instead of trusting ad hoc manual checks - COVERED by Task 1.
REQ: TEST-07 - COVERED by focused node, renderer, and boundary regressions plus the cross-route truth-state matrix.
REQ: UI-08 - COVERED by shared-naming and import-boundary enforcement plus multi-harness service or renderer checks.
REQ: UI-09 - COVERED by explicit unsupported, unknown, not-run, placeholder, and no-evidence assertions across every route.
RESEARCH: capability-aware rendering, no provider-specific branches, and focused test-project commands - COVERED.
CONTEXT: D-05 through D-18 - COVERED. No new functional scope beyond final hardening is introduced.
</source_coverage_audit>

<output>
Create `.planning/phases/06-harness-neutral-triage-ui/06-05-SUMMARY.md` when done.
</output>
