---
phase: 06-harness-neutral-triage-ui
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/main/app/run-audit-view-model-service.ts
  - src/main/ipc/channels.ts
  - src/main/ipc/handlers.ts
  - src/main/ipc/view-models.ts
  - src/preload/index.ts
  - src/preload/types.ts
  - src/renderer/routes/RunAuditRoute.tsx
  - src/renderer/routes/SessionsRoute.tsx
  - src/renderer/routes/SessionDetailRoute.tsx
  - src/renderer/components/triage/RunAuditSummaryRail.tsx
  - src/renderer/components/triage/RunAuditSection.tsx
  - src/renderer/components/triage/RunAuditEvidenceList.tsx
  - src/renderer/styles.css
  - tests/main/ipc/run-audit-view-model-service.test.ts
  - tests/renderer/run-audit-route.test.tsx
autonomous: true
requirements:
  - UI-05
  - UI-08
  - UI-09
must_haves:
  truths:
    - "Run Audit is a dedicated route or subview that groups evidence by claim vs evidence, verification, files changed, commands, cancellation or incompletion, git or GitHub state, capability gaps, and parser diagnostics."
    - "Run Audit consumes Phase 5 derived audit status, attention reasons, verification results, and supporting IDs from main-owned DTOs; renderer code must not re-derive audit precedence."
    - "Phase 7 git or GitHub fields stay explicit placeholders or no-evidence sections, not inferred repo truth."
    - "Empty or unsupported groups remain visible with explicit copy instead of disappearing silently."
  artifacts:
    - path: "src/main/app/run-audit-view-model-service.ts"
      provides: "Grouped run-audit DTO service with supporting evidence joins"
      contains: "RunAuditViewModelService"
    - path: "src/renderer/routes/RunAuditRoute.tsx"
      provides: "Dedicated Run Audit route with grouped evidence sections"
    - path: "tests/main/ipc/run-audit-view-model-service.test.ts"
      provides: "Proof that grouped audit DTOs stay truthful, sanitized, and placeholder-aware"
  key_links:
    - from: "src/main/app/run-audit-view-model-service.ts"
      to: "src/main/core/audit/types.ts"
      via: "projects shared run-audit status and attention reason vocabulary into grouped DTOs"
      pattern: "attentionReasons"
    - from: "src/main/app/run-audit-view-model-service.ts"
      to: "src/main/core/cache/file-backed-cache-store.ts"
      via: "supporting command, tool, message, and diagnostic IDs"
      pattern: "supporting"
    - from: "src/renderer/routes/SessionDetailRoute.tsx"
      to: "src/renderer/routes/RunAuditRoute.tsx"
      via: "deep-link from chronology page to grouped judgment page"
      pattern: "Open Run Audit"
---

<objective>
Build the Run Audit route and grouped evidence DTOs so users can review claim-vs-evidence truth through shared-core audit outputs instead of a long timeline or provider-specific heuristics.

Purpose: The product wedge is not just seeing a session's chronology; it is deciding whether a run is trustworthy based on shared audit classification, attention reasons, verification results, and evidence support.
Output: grouped run-audit DTOs, explicit route and preload methods, grouped renderer sections, and focused tests for placeholder-aware truthful rendering.
</objective>

<execution_context>
@/Users/rhishi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rhishi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-harness-neutral-triage-ui/06-CONTEXT.md
@.planning/phases/06-harness-neutral-triage-ui/06-RESEARCH.md
@.planning/phases/06-harness-neutral-triage-ui/06-UI-SPEC.md
@.planning/phases/06-harness-neutral-triage-ui/06-PATTERNS.md
@.planning/phases/06-harness-neutral-triage-ui/06-VALIDATION.md
@.planning/phases/06-harness-neutral-triage-ui/06-01-PLAN.md
@.planning/phases/06-harness-neutral-triage-ui/06-02-PLAN.md
@src/main/core/audit/types.ts
@src/main/core/verification/types.ts
@src/main/core/cache/file-backed-cache-store.ts
@src/main/ipc/{channels,handlers,view-models}.ts
@src/preload/{index,types}.ts
@src/renderer/routes/SessionDetailRoute.tsx
@src/renderer/styles.css
@tests/main/ipc/session-view-model-service.test.ts

<interfaces>
Existing seams to preserve:
- Run Audit uses Phase 5 `record.derived.sessions[].audit` and `.verification` as its source of truth. Do not recompute precedence in renderer code.
- The route is read-only and explanatory. It must not add rerun, retry, terminal, approve, reject, git, or PR mutation controls.
- Git and GitHub sections remain honest placeholders until shared providers exist in Phase 7.
- Session Detail owns chronology; Run Audit owns grouped judgment. Keep those responsibilities separate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add grouped Run Audit DTOs and dedicated evidence-review route</name>
  <files>src/main/app/run-audit-view-model-service.ts, src/main/ipc/channels.ts, src/main/ipc/handlers.ts, src/main/ipc/view-models.ts, src/preload/index.ts, src/preload/types.ts, src/renderer/routes/RunAuditRoute.tsx, src/renderer/routes/SessionsRoute.tsx, src/renderer/routes/SessionDetailRoute.tsx, src/renderer/components/triage/RunAuditSummaryRail.tsx, src/renderer/components/triage/RunAuditSection.tsx, src/renderer/components/triage/RunAuditEvidenceList.tsx, src/renderer/styles.css, tests/main/ipc/run-audit-view-model-service.test.ts, tests/renderer/run-audit-route.test.tsx</files>
  <read_first>06-CONTEXT decisions D-10 through D-18; 06-UI-SPEC Phase UI Scope, Layout Contract, State Vocabulary, Interaction Contract, and Copywriting Contract; 06-PATTERNS full file; src/main/core/audit/types.ts; src/main/core/verification/types.ts; src/main/core/cache/file-backed-cache-store.ts; src/main/ipc/view-models.ts; src/preload/index.ts; src/renderer/routes/SessionDetailRoute.tsx; src/renderer/styles.css</read_first>
  <behavior>
    - Test 1: node-side Run Audit DTOs group claim vs evidence, verification, files changed, commands, cancellation or incompletion, git or GitHub state, capability gaps, and parser diagnostics using Phase 5 supporting IDs and shared vocabularies.
    - Test 2: groups that are empty, unsupported, or not yet available in Phase 6 still render explicit headings and no-evidence or placeholder copy instead of disappearing.
    - Test 3: the route summary rail shows audit headline, verification headline, lifecycle, attention reasons, and capability warnings without recomputing audit status in the renderer.
    - Test 4: `/sessions/:sessionId/audit` loads through explicit preload and IPC methods and is reachable from Sessions and Session Detail via read-only drill-in actions.
    - Test 5: git or GitHub sections do not infer branch, dirty state, or PR truth from session text; they render Phase 7 placeholders or explicit missing-evidence state only.
  </behavior>
  <action>Create `src/main/app/run-audit-view-model-service.ts` to load one session, resolve its Phase 5 derived audit and verification entries, and join supporting command, tool-call, message, file-mutation, and diagnostic entities into grouped, sanitized DTO sections. Extend IPC, preload, and renderer contracts with a dedicated Run Audit method such as `getSessionRunAudit(request)`. Implement `RunAuditRoute.tsx` plus summary-rail and grouped-section components so each required section renders an explicit state even when there is no evidence or the capability is unsupported. Add read-only `Open Run Audit` drill-ins from the Sessions preview and Session Detail summary rail. Keep grouped section labels exactly aligned with the Phase 6 contract, and keep git or GitHub content limited to placeholders or unsupported or unknown states until Phase 7 providers exist. Do not add rerun buttons, shell controls, repo mutation, PR actions, generic preload calls, or renderer-side audit heuristics.</action>
  <verify>
    <automated>npm run test -- --project node tests/main/ipc/run-audit-view-model-service.test.ts</automated>
    <automated>npm run test -- --project renderer tests/renderer/run-audit-route.test.tsx tests/renderer/session-detail-route.test.tsx</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run test:boundaries</automated>
  </verify>
  <acceptance_criteria>`src/main/app/run-audit-view-model-service.ts` exists and emits grouped, sanitized Run Audit DTOs; `/sessions/:sessionId/audit` renders explicit grouped evidence sections through typed bridge methods only; Sessions and Session Detail link to Run Audit without merging route responsibilities; and git or GitHub evidence remains placeholder-driven in Phase 6.</acceptance_criteria>
  <done>Run Audit becomes a dedicated evidence-review surface built from shared-core audit truth instead of provider-specific or renderer-local heuristics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 5 derived audit truth -> grouped Run Audit DTOs | The route headline and grouped evidence must remain conservative, traceable, and explicit about missing capabilities. |
| Run Audit route -> user trust decisions | Placeholder git or GitHub state and partial evidence must not be presented as clean or verified truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Repudiation | grouped audit evidence | mitigate | Join supporting IDs in main and keep grouped sections traceable to commands, tool calls, messages, files, and diagnostics rather than renderer summaries only. |
| T-06-03-02 | Spoofing | git or GitHub section | mitigate | Render explicit Phase 7 placeholders or unsupported states only; never infer repo state from session text or final-answer claims. |
| T-06-03-03 | Tampering | renderer audit heuristics | mitigate | Keep audit grouping and section-state logic in main-owned services backed by Phase 5 derived cache truth. |
| T-06-03-SC | Tampering | UI package install | mitigate | Do not add dependencies for grouped audit UI; reuse existing CSS or already-approved primitives only. |
</threat_model>

<verification>
Plan-level checks:
- `npm run test -- --project node tests/main/ipc/run-audit-view-model-service.test.ts`
- `npm run test -- --project renderer tests/renderer/run-audit-route.test.tsx tests/renderer/session-detail-route.test.tsx`
- `npm run typecheck`
- `npm run test:boundaries`
</verification>

<success_criteria>
- Run Audit is a dedicated route with all required grouped sections.
- Grouped evidence is main-owned, typed, and traceable to supporting IDs.
- Placeholder or unsupported git and GitHub state stays honest until Phase 7.
</success_criteria>

<source_coverage_audit>
GOAL: users can review claim-vs-evidence truth through a dedicated Run Audit page instead of inferring it from the session timeline - COVERED by Task 1.
REQ: UI-05 - COVERED by grouped Run Audit sections and shared audit or verification DTO projection.
REQ: UI-08 - COVERED by main-owned grouped audit services and shared renderer logic with no provider-ID branching.
REQ: UI-09 - COVERED by explicit section states for unsupported, unknown, and no-evidence cases.
RESEARCH: grouped audit DTOs, placeholder git or GitHub handling, and separate judgment from chronology - COVERED.
CONTEXT: D-10 through D-18 - COVERED. Diagnostics-specific grouping remains deferred to 06-04.
</source_coverage_audit>

<output>
Create `.planning/phases/06-harness-neutral-triage-ui/06-03-SUMMARY.md` when done.
</output>
