---
phase: 06-harness-neutral-triage-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/main/app/session-detail-view-model-service.ts
  - src/main/ipc/channels.ts
  - src/main/ipc/handlers.ts
  - src/main/ipc/view-models.ts
  - src/preload/index.ts
  - src/preload/types.ts
  - src/renderer/routes/SessionDetailRoute.tsx
  - src/renderer/routes/SessionsRoute.tsx
  - src/renderer/components/SessionPreview.tsx
  - src/renderer/components/triage/SessionDetailSummaryRail.tsx
  - src/renderer/components/triage/SessionTimeline.tsx
  - src/renderer/components/triage/TimelineEventCard.tsx
  - src/renderer/styles.css
  - tests/main/ipc/session-detail-view-model-service.test.ts
  - tests/renderer/session-detail-route.test.tsx
autonomous: true
requirements:
  - UI-04
  - UI-08
  - UI-09
must_haves:
  truths:
    - "Session Detail is a dedicated route that owns chronology and does not collapse back into the Sessions list or Run Audit sections."
    - "The summary rail surfaces harness badge, project, session IDs, lifecycle, verification, run audit, attention reasons, and capability warnings from sanitized main-owned DTOs."
    - "The timeline mixes messages, lifecycle events, tool calls, shell commands, file mutations, output artifacts, and unknown raw evidence markers in chronological order."
    - "Shell output, artifact contents, and raw cache records stay sanitized; the detail route should expose short summaries and references only."
  artifacts:
    - path: "src/main/app/session-detail-view-model-service.ts"
      provides: "Sanitized session-detail DTO service over normalized plus derived cache data"
      contains: "SessionDetailViewModelService"
    - path: "src/renderer/routes/SessionDetailRoute.tsx"
      provides: "Dedicated Session Detail route with summary rail plus timeline"
    - path: "src/renderer/components/triage/SessionTimeline.tsx"
      provides: "Chronological mixed-evidence timeline renderer"
    - path: "tests/main/ipc/session-detail-view-model-service.test.ts"
      provides: "Proof that session detail DTOs stay chronological, sanitized, and adapter-neutral"
  key_links:
    - from: "src/main/app/session-detail-view-model-service.ts"
      to: "src/main/core/cache/file-backed-cache-store.ts"
      via: "joins normalized events with derived verification and audit summaries"
      pattern: "derived.sessions"
    - from: "src/renderer/routes/SessionsRoute.tsx"
      to: "src/renderer/routes/SessionDetailRoute.tsx"
      via: "drill-in action from fast triage to full timeline"
      pattern: "Open Session Detail"
    - from: "src/renderer/components/triage/SessionTimeline.tsx"
      to: "src/main/ipc/view-models.ts"
      via: "typed event rows and collapsed unknown markers"
      pattern: "timeline"
---

<objective>
Add the Session Detail route and service layer so users can inspect a single session's full chronological evidence without mixing chronology with Run Audit judgment.

Purpose: The denser Sessions triage surface from 06-01 should stay scan-first. Users still need a dedicated page that replays messages, lifecycle, tool, shell, file, and artifact evidence in order.
Output: session-detail DTOs, typed IPC and preload methods, timeline components, summary rail UI, and focused node plus renderer coverage.
</objective>

<execution_context>
@/Users/rhishi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rhishi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-harness-neutral-triage-ui/06-CONTEXT.md
@.planning/phases/06-harness-neutral-triage-ui/06-RESEARCH.md
@.planning/phases/06-harness-neutral-triage-ui/06-UI-SPEC.md
@.planning/phases/06-harness-neutral-triage-ui/06-PATTERNS.md
@.planning/phases/06-harness-neutral-triage-ui/06-VALIDATION.md
@.planning/phases/06-harness-neutral-triage-ui/06-01-PLAN.md
@src/main/app/session-view-model-service.ts
@src/main/core/cache/file-backed-cache-store.ts
@src/main/core/model/entities.ts
@src/main/ipc/{channels,handlers,view-models}.ts
@src/preload/{index,types}.ts
@src/renderer/routes/SessionsRoute.tsx
@src/renderer/components/SessionPreview.tsx
@src/renderer/styles.css
@tests/main/ipc/session-view-model-service.test.ts
@tests/renderer/sessions-route.test.tsx

<interfaces>
Existing seams to preserve:
- Session Detail consumes sanitized DTOs only; renderer must not traverse raw cache records or adapter-private fields.
- The Sessions route remains the fast queue and only links out to Session Detail; it does not absorb the full timeline.
- Run Audit remains a separate route that owns grouped evidence judgment. Do not bury grouped audit sections inside the timeline.
- Unknown or partially parsed evidence is still shown explicitly, but only through safe summaries and labels.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sanitized Session Detail DTOs, route, and mixed-evidence timeline</name>
  <files>src/main/app/session-detail-view-model-service.ts, src/main/ipc/channels.ts, src/main/ipc/handlers.ts, src/main/ipc/view-models.ts, src/preload/index.ts, src/preload/types.ts, src/renderer/routes/SessionDetailRoute.tsx, src/renderer/routes/SessionsRoute.tsx, src/renderer/components/SessionPreview.tsx, src/renderer/components/triage/SessionDetailSummaryRail.tsx, src/renderer/components/triage/SessionTimeline.tsx, src/renderer/components/triage/TimelineEventCard.tsx, src/renderer/styles.css, tests/main/ipc/session-detail-view-model-service.test.ts, tests/renderer/session-detail-route.test.tsx</files>
  <read_first>06-CONTEXT decisions D-09 through D-14; 06-UI-SPEC Phase UI Scope, Layout Contract, State Vocabulary, and Interaction Contract; 06-PATTERNS Session Detail rows; src/main/core/model/entities.ts; src/main/app/session-view-model-service.ts; src/main/ipc/view-models.ts; src/preload/index.ts; src/renderer/routes/SessionsRoute.tsx; src/renderer/components/SessionPreview.tsx; src/renderer/styles.css</read_first>
  <behavior>
    - Test 1: node-side session-detail DTOs include summary-rail data for harness, project, session IDs, lifecycle, verification, run audit, attention reasons, and capability warnings while staying free of raw artifact paths or unsanitized shell output bodies.
    - Test 2: timeline rows cover user, assistant, system or harness messages, lifecycle events, tool calls, shell commands, file mutations, output artifacts, and explicit unknown raw evidence markers in chronological order.
    - Test 3: shell commands, tool calls, file mutations, and output artifacts render collapsed summaries first, with inline expansion only for sanitized details defined by the DTO.
    - Test 4: `/sessions/:sessionId` loads through the typed bridge, shows the exact `Open Session Detail` drill-in from the Sessions surface, and keeps capability warnings and attention reasons explicit.
    - Test 5: provider IDs affect display labels only; timeline grouping and summary logic remain shared and harness-neutral.
  </behavior>
  <action>Create `src/main/app/session-detail-view-model-service.ts` to load one session from merged normalized records, join in its project label and derived verification or audit summary, and project a typed timeline DTO ordered by event ordinal then timestamp. Extend `src/main/ipc/view-models.ts`, `channels.ts`, `handlers.ts`, `src/preload/index.ts`, and `src/preload/types.ts` with an explicit session-detail method such as `getSessionDetail(request)`. In renderer code, add `SessionDetailRoute.tsx` plus focused summary-rail and timeline components under `src/renderer/components/triage/`, and wire Sessions preview or route actions to navigate to `/sessions/:sessionId`. Use collapsed summaries for long shell, tool, and artifact rows; keep unknown raw evidence visible as a labeled timeline row instead of dropping it. Do not expose full artifact bodies, raw filesystem paths, adapter-private raw events, provider-specific layout branches, or grouped Run Audit sections inside the timeline route.</action>
  <verify>
    <automated>npm run test -- --project node tests/main/ipc/session-detail-view-model-service.test.ts</automated>
    <automated>npm run test -- --project renderer tests/renderer/session-detail-route.test.tsx tests/renderer/sessions-route.test.tsx</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run test:boundaries</automated>
  </verify>
  <acceptance_criteria>`src/main/app/session-detail-view-model-service.ts` exists with strict DTO parsing; `/sessions/:sessionId` renders a summary rail plus chronological mixed timeline through typed preload methods only; unknown raw evidence stays explicit; and shell or artifact detail never leaks unsanitized raw output or path data.</acceptance_criteria>
  <done>Session Detail becomes the dedicated chronology surface for Phase 6 while Sessions remains the fast triage queue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized events and derived truth -> session-detail DTOs | Rich per-session evidence becomes user-visible and must stay chronological, sanitized, and explicit about uncertainty. |
| session-detail route -> typed preload contract | New drill-in behavior must stay on explicit bridge methods and cannot widen renderer access to raw IPC or cache data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | session-detail DTOs | mitigate | Expose only short summaries, labels, IDs, and safe metadata; never return raw artifact bodies, raw shell output, or unsafe filesystem paths. |
| T-06-02-02 | Tampering | timeline grouping | mitigate | Build chronology in main from normalized event order and stable IDs; renderer only renders typed rows and does not invent its own evidence ordering heuristics. |
| T-06-02-03 | Spoofing | provider-specific detail rendering | mitigate | Keep route logic shared across adapters and allow provider differences only in display labels or capability metadata. |
| T-06-02-SC | Tampering | UI package install | mitigate | Reuse existing CSS and approved primitives only; do not add new packages during this route implementation. |
</threat_model>

<verification>
Plan-level checks:
- `npm run test -- --project node tests/main/ipc/session-detail-view-model-service.test.ts`
- `npm run test -- --project renderer tests/renderer/session-detail-route.test.tsx tests/renderer/sessions-route.test.tsx`
- `npm run typecheck`
- `npm run test:boundaries`
</verification>

<success_criteria>
- Session Detail is a dedicated route with a sanitized summary rail and chronological mixed timeline.
- Sessions links into Session Detail without absorbing full-detail scope.
- Unknown or partial evidence stays visible without leaking unsafe raw data.
</success_criteria>

<source_coverage_audit>
GOAL: users can inspect one run's chronology through a dedicated Session Detail page instead of overloading the Sessions queue - COVERED by Task 1.
REQ: UI-04 - COVERED by the summary rail and mixed-evidence timeline route.
REQ: UI-08 - COVERED by shared session-detail DTOs and route logic with no provider-ID branches.
REQ: UI-09 - COVERED by explicit unknown or unsupported badges and timeline rows for partial evidence.
RESEARCH: main-owned detail DTOs, separate chronology from judgment, and collapsed detail rows - COVERED.
CONTEXT: D-09 through D-14 - COVERED. Grouped Run Audit judgment remains deferred to 06-03.
</source_coverage_audit>

<output>
Create `.planning/phases/06-harness-neutral-triage-ui/06-02-SUMMARY.md` when done.
</output>
