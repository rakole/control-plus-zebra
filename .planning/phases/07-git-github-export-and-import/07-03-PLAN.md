---
phase: 07-git-github-export-and-import
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/main/core/archive/archive-manifest.ts
  - src/main/core/archive/archive-exporter.ts
  - src/main/core/security/safe-filesystem.ts
  - src/main/app/archive-export-service.ts
  - src/main/ipc/channels.ts
  - src/main/ipc/handlers.ts
  - src/main/ipc/view-models.ts
  - src/preload/index.ts
  - src/preload/types.ts
  - src/renderer/routes/ProjectsRoute.tsx
  - src/renderer/routes/RunAuditRoute.tsx
  - tests/main/core/archive-exporter.test.ts
  - tests/main/core/safe-filesystem.test.ts
  - tests/main/ipc/ipc-handlers.test.ts
  - tests/renderer/projects-route.test.tsx
  - tests/renderer/run-audit-route.test.tsx
autonomous: true
requirements:
  - GIT-04
  - GIT-06
must_haves:
  truths:
    - "D-09: Export defaults to normalized metadata, normalized entities, and diagnostics. Raw artifacts remain opt-in instead of included by default."
    - "D-10: Raw artifact export is allowed only for artifacts already indexed and readable through the shared safe-filesystem allowlist."
    - "D-11: Every raw-inclusive export warns that transcripts, sidecars, repo paths, and command output may contain sensitive data, and the manifest records whether raw data was included."
    - "D-12: Archive format remains read-only and harness-neutral: manifest plus normalized payloads plus optional raw-artifact bundle, with no executable scripts, mutable source configuration, or live repo bindings."
  artifacts:
    - path: "src/main/core/archive/archive-exporter.ts"
      provides: "Harness-neutral archive writer built from cache, registry, diagnostics, and optional indexed raw artifacts"
      contains: "ArchiveExporter"
    - path: "src/main/app/archive-export-service.ts"
      provides: "Main-owned export orchestration and destination selection behind typed IPC"
      contains: "ArchiveExportService"
    - path: "src/main/ipc/view-models.ts"
      provides: "Typed export request and response contracts, including raw-artifact opt-in and warning acknowledgement"
      contains: "createArchiveRequestSchema"
    - path: "src/renderer/routes/ProjectsRoute.tsx"
      provides: "Project-level export entrypoint and privacy-warning confirmation flow"
      contains: "Export Project Archive"
  key_links:
    - from: "src/main/app/archive-export-service.ts"
      to: "src/main/core/archive/archive-exporter.ts"
      via: "main-owned archive assembly over shared cache and registry data"
      pattern: "createArchive"
    - from: "src/main/ipc/handlers.ts"
      to: "src/preload/types.ts"
      via: "typed export bridge with no generic invoke escape hatch"
      pattern: "export:createArchive"
    - from: "src/renderer/routes/RunAuditRoute.tsx"
      to: "src/main/ipc/view-models.ts"
      via: "session-level export confirmation and raw warning state"
      pattern: "Export Session Archive"
---

<objective>
Add the harness-neutral export path for Phase 7 so users can create portable read-only archives of normalized data and optional indexed raw artifacts without turning export into a general local file copier.

Purpose: Export is the portability half of Phase 7. It must preserve the app's privacy and safety posture while giving users a predictable archive format that later import can read back through the same shared seams.
Output: a manifest-backed archive exporter, typed IPC and preload contracts, project and run-audit export entrypoints, and tests for privacy warning, raw-artifact gating, and manifest integrity.
</objective>

<execution_context>
@/Users/rhishi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rhishi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-git-github-export-and-import/07-CONTEXT.md
@.planning/phases/07-git-github-export-and-import/07-RESEARCH.md
@.planning/phases/07-git-github-export-and-import/07-UI-SPEC.md
@.planning/phases/07-git-github-export-and-import/07-VALIDATION.md
@.planning/phases/07-git-github-export-and-import/07-01-PLAN.md
@.spec/spec-from-5.5-revision-1.md
@.spec/additional-instructions.md
@src/main/app/workbench-runtime.ts
@src/main/core/{cache/file-backed-cache-store.ts,ingestion/raw-artifact-index.ts,registry/source-registry.ts,security/safe-filesystem.ts}
@src/main/ipc/{channels,handlers,view-models}.ts
@src/preload/{index,types}.ts
@src/renderer/routes/{ProjectsRoute.tsx,RunAuditRoute.tsx}
@tests/main/core/{safe-filesystem.test.ts,file-backed-cache-store.test.ts}
@tests/main/ipc/ipc-handlers.test.ts
@tests/renderer/{projects-route.test.tsx,run-audit-route.test.tsx}

<interfaces>
Existing seams to preserve:
- Export is main-owned and read-only from the app's perspective. Renderer routes trigger typed requests only and never enumerate raw artifact paths themselves.
- Raw artifacts may be included only when they already exist in the shared raw-artifact index and pass the existing safe-filesystem allowlist.
- Keep archive format harness-neutral and non-executable so Plan 07-04 can import it as a plain read-only data source.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add manifest-backed export with privacy warnings and indexed raw-artifact gating</name>
  <files>src/main/core/archive/archive-manifest.ts, src/main/core/archive/archive-exporter.ts, src/main/core/security/safe-filesystem.ts, src/main/app/archive-export-service.ts, src/main/ipc/channels.ts, src/main/ipc/handlers.ts, src/main/ipc/view-models.ts, src/preload/index.ts, src/preload/types.ts, src/renderer/routes/ProjectsRoute.tsx, src/renderer/routes/RunAuditRoute.tsx, tests/main/core/archive-exporter.test.ts, tests/main/core/safe-filesystem.test.ts, tests/main/ipc/ipc-handlers.test.ts, tests/renderer/projects-route.test.tsx, tests/renderer/run-audit-route.test.tsx</files>
  <read_first>07-CONTEXT decisions D-09 through D-12; 07-RESEARCH Summary, Pattern 4, and Don't Hand-Roll items; 07-UI-SPEC Layout Contract, Interaction Contract, and Copywriting Contract; src/main/core/ingestion/raw-artifact-index.ts; src/main/core/security/safe-filesystem.ts; src/main/ipc/handlers.ts; src/preload/types.ts; src/renderer/routes/ProjectsRoute.tsx; src/renderer/routes/RunAuditRoute.tsx</read_first>
  <behavior>
    - Test 1: normalized data and diagnostics export successfully without raw artifacts by default and record manifest metadata that preserves adapter, source, and inclusion mode.
    - Test 2: raw-artifact export is possible only when selected artifacts are already indexed and allowlisted; arbitrary source-root file copying remains blocked.
    - Test 3: project and session export entrypoints show explicit privacy-warning copy and keep the raw checkbox disabled or unchecked until the user opts in.
    - Test 4: export writes a portable read-only archive with no executable scripts, live repo bindings, or mutable source configuration.
    - Test 5: export failures stay sanitized at the IPC boundary and do not leak raw local filesystem details back into renderer DTOs.
  </behavior>
  <action>Create `src/main/core/archive/archive-manifest.ts` and `src/main/core/archive/archive-exporter.ts` so export packages manifest metadata, normalized cache data, diagnostics, and optional indexed raw artifacts into a harness-neutral archive format. Add `src/main/app/archive-export-service.ts` plus typed `export:createArchive` request and response shapes in `src/main/ipc/channels.ts`, `src/main/ipc/handlers.ts`, `src/main/ipc/view-models.ts`, `src/preload/index.ts`, and `src/preload/types.ts`, keeping every bridge method explicit. Update `src/renderer/routes/ProjectsRoute.tsx` and `src/renderer/routes/RunAuditRoute.tsx` with `Export Project Archive` and `Export Session Archive` entrypoints that default to Normalized Only, surface the privacy warning, and keep raw export disabled when indexed artifacts are unavailable. Extend safe-filesystem and export tests so raw inclusion only reads indexed allowlisted artifacts and renderer code never sees raw path enumeration logic. Do not add background export jobs, adapter-specific archive formats, silent raw inclusion, raw path browsing from the renderer, or archive editing behavior.</action>
  <verify>
    <automated>npm run test -- --project node tests/main/core/archive-exporter.test.ts tests/main/core/safe-filesystem.test.ts tests/main/ipc/ipc-handlers.test.ts</automated>
    <automated>npm run test -- --project renderer tests/renderer/projects-route.test.tsx tests/renderer/run-audit-route.test.tsx</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run test:boundaries</automated>
  </verify>
  <acceptance_criteria>`src/main/core/archive/archive-exporter.ts` exists and writes a manifest-backed harness-neutral archive; raw-artifact export stays opt-in and allowlist-gated; the manifest records whether raw data was included and the warning was acknowledged; and Projects plus Run Audit expose explicit export entrypoints without opening generic file-copy behavior.</acceptance_criteria>
  <done>Phase 7 can produce portable, privacy-aware archives through main-owned export flows that stay consistent with the app's read-only and allowlist boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized cache and indexed raw artifacts -> archive file | Export must copy only the intended normalized payloads and explicitly approved raw artifacts. |
| renderer confirmation flow -> main-owned archive write | Privacy-warning acknowledgement and inclusion mode must survive the bridge without becoming a generic file-write API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Information Disclosure | raw-artifact export | mitigate | Keep raw export opt-in, warn explicitly about sensitive transcript and path data, and allow only indexed allowlisted artifacts. |
| T-07-03-02 | Tampering | archive assembly | mitigate | Build archives only from shared cache, registry, diagnostics, and raw-artifact index data; do not permit arbitrary renderer-supplied file lists. |
| T-07-03-03 | Repudiation | manifest and warning state | mitigate | Record raw inclusion mode and warning acknowledgement in the manifest so archive consumers and future imports know what was shared. |
| T-07-03-SC | Tampering | Phase 7 scope boundary | mitigate | Keep this plan to archive creation only; import registration and read-only source behavior stay in Plan 07-04. |
</threat_model>

<verification>
Plan-level checks:
- `npm run test -- --project node tests/main/core/archive-exporter.test.ts tests/main/core/safe-filesystem.test.ts tests/main/ipc/ipc-handlers.test.ts`
- `npm run test -- --project renderer tests/renderer/projects-route.test.tsx tests/renderer/run-audit-route.test.tsx`
- `npm run typecheck`
- `npm run test:boundaries`
</verification>

<success_criteria>
- Export writes a harness-neutral archive format from shared cache and registry data.
- Raw-artifact inclusion remains opt-in, warned, and safe-filesystem-gated.
- Projects and Run Audit expose explicit export actions without widening into generic file-copy or mutation behavior.
</success_criteria>

<source_coverage_audit>
GOAL: add portable archives without mutating live sources or leaking sensitive local data by default - COVERED by Task 1.
REQ: GIT-04 - COVERED by manifest-backed archive export over shared source, session, and diagnostic data.
REQ: GIT-06 - COVERED by raw-artifact opt-in, privacy-warning acknowledgement, and allowlist-only raw inclusion rules.
RESEARCH: harness-neutral archive format, shared-core export ownership, and raw-artifact index reuse - COVERED.
CONTEXT: D-09 through D-12 - COVERED. Import registration remains Plan 07-04.
</source_coverage_audit>

<output>
Create `.planning/phases/07-git-github-export-and-import/07-03-SUMMARY.md` when done.
</output>
