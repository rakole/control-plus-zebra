---
phase: 07-git-github-export-and-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/core/git/root-confidence.ts
  - src/main/core/git/git-snapshot-provider.ts
  - src/main/core/cache/file-backed-cache-store.ts
  - src/main/core/ingestion/scanner.ts
  - src/main/app/triage-view-model-service.ts
  - src/main/app/run-audit-view-model-service.ts
  - src/main/ipc/view-models.ts
  - src/renderer/routes/ProjectsRoute.tsx
  - src/renderer/routes/RunAuditRoute.tsx
  - tests/main/core/git-snapshot-provider.test.ts
  - tests/main/core/scanner-cache.test.ts
  - tests/main/ipc/triage-view-model-service.test.ts
  - tests/main/ipc/run-audit-view-model-service.test.ts
  - tests/renderer/projects-route.test.tsx
  - tests/renderer/run-audit-route.test.tsx
autonomous: true
requirements:
  - GIT-01
  - GIT-02
must_haves:
  truths:
    - "D-01: Shared git inspection may use only validated repository roots. `observed` candidates may be validated, but `inferred` and `unknown` roots never execute git commands."
    - "D-02: Git snapshot data is project-scoped shared state derived in main and reused across Projects and Run Audit instead of being recomputed per renderer route or session load."
    - "D-03: Phase 7 git scope is limited to fixed read-only fields: branch, HEAD SHA, dirty state, changed and untracked counts, additions and deletions, and remote URL."
    - "D-04: Missing git, non-repo roots, validation mismatch, missing git, or timeouts degrade to explicit Available, Unknown, or Unsupported DTO states with diagnostics instead of fake clean repo truth."
  artifacts:
    - path: "src/main/core/git/git-snapshot-provider.ts"
      provides: "Validated root resolution and fixed read-only git snapshot collection"
      contains: "GitSnapshotProvider"
    - path: "src/main/core/cache/file-backed-cache-store.ts"
      provides: "Project-scoped derived snapshot persistence alongside existing session-derived cache data"
      contains: "DerivedCacheRecord"
    - path: "src/main/app/triage-view-model-service.ts"
      provides: "Projects DTOs backed by shared git snapshot truth instead of Phase 6 placeholders"
      contains: "listProjects"
    - path: "src/main/app/run-audit-view-model-service.ts"
      provides: "Run Audit Git section backed by shared project snapshot truth"
      contains: "getRunAudit"
  key_links:
    - from: "src/main/core/ingestion/scanner.ts"
      to: "src/main/core/git/git-snapshot-provider.ts"
      via: "scan-time project snapshot derivation and persistence"
      pattern: "scanSource"
    - from: "src/main/app/triage-view-model-service.ts"
      to: "src/main/ipc/view-models.ts"
      via: "typed project summary truth-state fields"
      pattern: "projectSummaryViewModelSchema"
    - from: "src/main/app/run-audit-view-model-service.ts"
      to: "src/renderer/routes/RunAuditRoute.tsx"
      via: "shared Git section DTO rendering"
      pattern: "git-github"
---

<objective>
Build the Phase 7 foundation for trustworthy repo context by adding a shared read-only git provider, root-confidence gating, and project-scoped git snapshots that replace the current Phase 6 placeholders in Projects and Run Audit.

Purpose: Phase 6 intentionally left branch, HEAD, dirty-state, and PR fields as placeholders. Phase 7 must upgrade those surfaces without violating the product's read-only, renderer-safe, and truth-state rules.
Output: main-owned git snapshot services, scan-time project snapshot persistence, typed Projects and Run Audit DTOs, and focused node and renderer tests for repo-truth degradation paths.
</objective>

<execution_context>
@/Users/rhishi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rhishi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-git-github-export-and-import/07-CONTEXT.md
@.planning/phases/07-git-github-export-and-import/07-RESEARCH.md
@.planning/phases/07-git-github-export-and-import/07-UI-SPEC.md
@.planning/phases/07-git-github-export-and-import/07-VALIDATION.md
@.planning/phases/06-harness-neutral-triage-ui/06-CONTEXT.md
@.planning/phases/05-shared-shell-verification-and-run-audit/05-CONTEXT.md
@.spec/spec-from-5.5-revision-1.md
@.spec/additional-instructions.md
@src/main/app/{triage-view-model-service.ts,run-audit-view-model-service.ts,workbench-runtime.ts}
@src/main/core/{cache/file-backed-cache-store.ts,ingestion/scanner.ts,model/entities.ts,security/safe-filesystem.ts}
@src/main/ipc/view-models.ts
@src/renderer/routes/{ProjectsRoute.tsx,RunAuditRoute.tsx}
@tests/main/core/scanner-cache.test.ts
@tests/main/ipc/{triage-view-model-service.test.ts,run-audit-view-model-service.test.ts}
@tests/renderer/{projects-route.test.tsx,run-audit-route.test.tsx}

<interfaces>
Existing seams to preserve:
- Renderer routes still consume typed IPC view models only. They must not execute `git`, derive repo truth from session shell text, or import shared-core internals.
- Shared repo truth belongs in `src/main/core/**` and main-owned services. Adapters remain evidence-only, including Gemini `.project_root` artifacts.
- Phase 7 git state is project-scoped and read-only. Do not add diff bodies, patch previews, mutable git actions, or provider-specific renderer branches here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared git snapshots and replace placeholder repo truth in Projects and Run Audit</name>
  <files>src/main/core/git/root-confidence.ts, src/main/core/git/git-snapshot-provider.ts, src/main/core/cache/file-backed-cache-store.ts, src/main/core/ingestion/scanner.ts, src/main/app/triage-view-model-service.ts, src/main/app/run-audit-view-model-service.ts, src/main/ipc/view-models.ts, src/renderer/routes/ProjectsRoute.tsx, src/renderer/routes/RunAuditRoute.tsx, tests/main/core/git-snapshot-provider.test.ts, tests/main/core/scanner-cache.test.ts, tests/main/ipc/triage-view-model-service.test.ts, tests/main/ipc/run-audit-view-model-service.test.ts, tests/renderer/projects-route.test.tsx, tests/renderer/run-audit-route.test.tsx</files>
  <read_first>07-CONTEXT decisions D-01 through D-04; 07-RESEARCH Summary, Pattern 1, Pattern 2, and Pitfalls 1 through 2; 07-UI-SPEC Phase UI Scope, State Vocabulary, Interaction Contract, and Copywriting Contract; src/main/core/cache/file-backed-cache-store.ts; src/main/core/ingestion/scanner.ts; src/main/app/triage-view-model-service.ts; src/main/app/run-audit-view-model-service.ts; src/main/ipc/view-models.ts</read_first>
  <behavior>
    - Test 1: validated repo roots produce shared branch, HEAD, dirty-state, change-count, additions and deletions, and remote URL snapshots without renderer-side recomputation.
    - Test 2: `observed` candidates may be validated, but `inferred` and `unknown` inputs never run git commands and instead surface explicit Unknown or Unsupported fields with diagnostics.
    - Test 3: repo-top-level mismatch, missing git, timeout, or non-repo directories never flatten into Clean, `0`, or a missing section in Projects or Run Audit.
    - Test 4: Projects and Run Audit render the same shared git snapshot truth, so route-level labels and hints stay consistent across both surfaces.
    - Test 5: no adapter-private root logic or renderer command execution path is introduced while replacing the Phase 6 placeholders.
  </behavior>
  <action>Create `src/main/core/git/root-confidence.ts` and `src/main/core/git/git-snapshot-provider.ts` to resolve validated repository roots and collect only the fixed read-only git fields allowed in Phase 7. Extend `src/main/core/cache/file-backed-cache-store.ts` and `src/main/core/ingestion/scanner.ts` with project-scoped derived git snapshots keyed by normalized project identity so scan-time derivation remains the single source of truth. Replace the current Phase 6 placeholders in `src/main/app/triage-view-model-service.ts`, `src/main/app/run-audit-view-model-service.ts`, `src/main/ipc/view-models.ts`, `src/renderer/routes/ProjectsRoute.tsx`, and `src/renderer/routes/RunAuditRoute.tsx` with explicit Available, Unknown, and Unsupported git fields driven only by the shared snapshot. Add focused provider, scanner, IPC, and renderer tests proving root-confidence gating, remote/top-level mismatch handling, and timeout or missing-git degradation. Do not add GitHub calls, repo mutation, diff bodies, patch previews, generic shell helpers, or adapter-private repo heuristics in this plan.</action>
  <verify>
    <automated>npm run test -- --project node tests/main/core/git-snapshot-provider.test.ts tests/main/core/scanner-cache.test.ts tests/main/ipc/triage-view-model-service.test.ts tests/main/ipc/run-audit-view-model-service.test.ts</automated>
    <automated>npm run test -- --project renderer tests/renderer/projects-route.test.tsx tests/renderer/run-audit-route.test.tsx</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run test:boundaries</automated>
  </verify>
  <acceptance_criteria>`src/main/core/git/git-snapshot-provider.ts` exists and collects only the approved read-only git fields after validated root resolution; derived cache records persist project-scoped git snapshots; Projects and Run Audit render shared git truth instead of Phase 6 placeholders; and unsupported or unknown repo states never render as Clean, `0`, or a hidden section.</acceptance_criteria>
  <done>Phase 7 has a trustworthy shared git foundation that upgrades repo truth without weakening the renderer boundary or the product's honesty standard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter-provided root evidence -> shared git provider | Weak or mismatched root evidence must not produce overstated repo truth. |
| fixed read-only git commands -> user-facing DTOs | Command failures, timeouts, and non-repo paths must surface as explicit uncertainty rather than fake clean state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Spoofing | root-confidence gating | mitigate | Validate repo roots before publishing git snapshots; `inferred` and `unknown` roots never execute git commands. |
| T-07-01-02 | Tampering | git truth-state projection | mitigate | Model repo fields as typed Available, Unknown, and Unsupported states and assert in tests that missing evidence never renders as Clean or zero. |
| T-07-01-03 | Information Disclosure | shared command execution | mitigate | Use fixed read-only git commands only, collect narrow summary fields, and avoid diff bodies or raw command dumps in renderer DTOs. |
| T-07-01-SC | Tampering | Phase 7 scope boundary | mitigate | Keep this plan limited to shared git snapshots; GitHub, export, and import remain later Phase 7 plans. |
</threat_model>

<verification>
Plan-level checks:
- `npm run test -- --project node tests/main/core/git-snapshot-provider.test.ts tests/main/core/scanner-cache.test.ts tests/main/ipc/triage-view-model-service.test.ts tests/main/ipc/run-audit-view-model-service.test.ts`
- `npm run test -- --project renderer tests/renderer/projects-route.test.tsx tests/renderer/run-audit-route.test.tsx`
- `npm run typecheck`
- `npm run test:boundaries`
</verification>

<success_criteria>
- Shared git snapshots are project-scoped, root-confidence-gated, and persisted in main-owned derived cache records.
- Projects and Run Audit render real repo truth instead of Phase 6 placeholders.
- Missing or unsafe repo evidence remains explicit and never collapses into fake clean state.
</success_criteria>

<source_coverage_audit>
GOAL: enrich audit views with safe read-only repo context without mutating repositories or overstating weak repo evidence - COVERED by Task 1.
REQ: GIT-01 - COVERED by shared git snapshot provider, fixed read-only fields, and project-scoped derivation.
REQ: GIT-02 - COVERED by root-confidence gating, validated-root execution rules, and explicit Unknown or Unsupported degradation.
RESEARCH: shared project snapshots, root-confidence resolution, no renderer-side git logic, and fixed-command-only collection - COVERED.
CONTEXT: D-01 through D-04 - COVERED. GitHub, export, and import remain later Phase 7 plans.
</source_coverage_audit>

<output>
Create `.planning/phases/07-git-github-export-and-import/07-01-SUMMARY.md` when done.
</output>
