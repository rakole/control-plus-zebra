---
phase: 04-gemini-cli-adapter-end-to-end
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/main/adapters/gemini-cli/normalize.ts
  - src/main/adapters/gemini-cli/index.ts
  - tests/adapters/gemini-cli/gemini-normalize.test.ts
  - tests/main/core/scanner-cache.test.ts
  - tests/main/ipc/session-view-model-service.test.ts
autonomous: true
requirements:
  - ADPT-05
  - ADPT-06
must_haves:
  truths:
    - "Gemini raw events map into shared projects, sessions, events, messages, tool calls, shell evidence, output artifacts, file mutations, and diagnostics only."
    - "Contradictory lifecycle or summary metadata becomes diagnostics rather than silent trust in a single raw field."
    - "Sidecar content stays lazy through `loadOutputArtifact()` and indexed artifact reads; normalized cache records keep references instead of bulk sidecar bodies."
  artifacts:
    - path: "src/main/adapters/gemini-cli/normalize.ts"
      provides: "Gemini raw-event to shared normalized-entity mapping"
      contains: "normalizeGeminiCliEvents"
    - path: "src/main/adapters/gemini-cli/index.ts"
      provides: "Adapter assembly, lazy sidecar loading, and watch-plan surface"
      contains: "geminiCliAdapter"
    - path: "tests/main/ipc/session-view-model-service.test.ts"
      provides: "Shared pipeline proof that Gemini sessions flow into existing session view models"
  key_links:
    - from: "src/main/adapters/gemini-cli/normalize.ts"
      to: "src/main/core/model/entities.ts"
      via: "shared normalized entity creation"
      pattern: "Session|SessionEvent|ToolCall|ShellCommandEvidence"
    - from: "src/main/adapters/gemini-cli/index.ts"
      to: "src/main/core/security/safe-filesystem.ts"
      via: "lazy output-artifact loading through indexed artifact reads"
      pattern: "loadOutputArtifact"
---

<objective>
Map Gemini raw events into the shared normalized model and prove the resulting records flow through the existing scanner, cache, and Sessions view-model pipeline without Gemini-specific shared behavior.

Purpose: Phase 4 only succeeds when Gemini evidence becomes shared normalized data that the core and renderer already know how to consume, with uncertainty preserved as diagnostics and sidecar bodies loaded lazily.
Output: Normalization layer, lazy sidecar loading, and integration tests over scanner/cache/session view models.
</objective>

<execution_context>
@/Users/rhishi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rhishi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-gemini-cli-adapter-end-to-end/04-CONTEXT.md
@.planning/phases/04-gemini-cli-adapter-end-to-end/04-RESEARCH.md
@src/main/core/adapter-contract/types.ts
@src/main/core/model/entities.ts
@src/main/core/model/identifiers.ts
@src/main/core/ingestion/scanner.ts
@src/main/core/ingestion/normalization-validator.ts
@src/main/app/session-view-model-service.ts
@src/main/adapters/fake-test/normalize.ts
@tests/contract/run-adapter-contract.ts

<interfaces>
Normalization rules to preserve:
- Every normalized entity created from Gemini data carries `adapterId` and `sourceId`.
- Shared entities stay harness-neutral; all Gemini-specific raw semantics must be consumed inside adapter-private normalization.
- `loadOutputArtifact()` may read only indexed artifact refs and must support both plain-text and JSON-wrapped outputs.
- Shared core remains the owner of verification, run audit, and attention reasoning; normalization must not add forbidden conclusion fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize Gemini raw events and prove shared pipeline flow</name>
  <files>src/main/adapters/gemini-cli/normalize.ts, src/main/adapters/gemini-cli/index.ts, tests/adapters/gemini-cli/gemini-normalize.test.ts, tests/main/core/scanner-cache.test.ts, tests/main/ipc/session-view-model-service.test.ts</files>
  <read_first>04-CONTEXT decisions D-09 through D-14; 04-RESEARCH sections "What Phase 4 Must Prove", "Validation Architecture", and "Security and Truth Guardrails"; fake-test normalize pattern; current session-view-model service</read_first>
  <behavior>
    - Test 1: normalization emits shared project, session, event, message, tool-call, shell-command, output-artifact, and file-mutation entities with stable IDs and correct relationships.
    - Test 2: contradictory lifecycle or summary metadata produces diagnostics while preserving the timeline-derived evidence.
    - Test 3: sidecar-backed output artifacts are represented as references in normalized output and `loadOutputArtifact()` returns plain-text or JSON-wrapped content through indexed artifact reads only.
    - Test 4: scanner/cache integration with a Gemini fixture produces cached normalized records that merge alongside existing fake-adapter records.
    - Test 5: session view-model service can render a Gemini-backed session summary/preview without new provider-specific branches or forbidden conclusion fields.
  </behavior>
  <action>Implement adapter-private normalization that consumes Gemini raw-event kinds and produces only shared-core entities plus diagnostics. Use deterministic ID helpers, preserve chronology from chat evidence, attach diagnostics for missing sidecars or contradictory lifecycle evidence, and keep shell/file/tool evidence faithful without trying to classify verification or run-audit state. Extend the assembled adapter in `index.ts` with `loadOutputArtifact()` and a watch-plan surface consistent with descriptor capabilities. Add focused integration tests over scanner/cache and session view models so the first real adapter proves it can flow through the existing runtime, not just isolated unit tests.</action>
  <verify>
    <automated>npm run test -- tests/adapters/gemini-cli/gemini-normalize.test.ts tests/main/core/scanner-cache.test.ts tests/main/ipc/session-view-model-service.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Gemini normalized output uses only shared entity types, scanner/cache/session view-model tests prove end-to-end flow, lazy sidecar loading honors artifact allowlists, and forbidden verification/run-audit conclusions remain absent.</acceptance_criteria>
  <done>ADPT-05 is implemented and the normalization half of ADPT-06 is proven through shared-pipeline tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter-private raw events -> shared normalized model | Gemini-specific semantics must be translated without leaking provider-shaped fields or false conclusions. |
| normalized output artifacts -> lazy artifact reads | Sidecar content access must remain constrained to indexed artifact paths only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Spoofing | normalized entity mapping | mitigate | Use shared ID helpers and relationship validation so projects, sessions, events, tools, artifacts, and file mutations remain adapter/source-scoped and deterministic. |
| T-04-03-02 | Tampering | lifecycle/summary interpretation | mitigate | Preserve evidence from the timeline, attach diagnostics for contradictions, and avoid adapter-level verification or run-audit conclusions. |
| T-04-03-03 | Information Disclosure | sidecar loading | mitigate | Implement `loadOutputArtifact()` through indexed artifact refs and safe filesystem helpers only; do not inline large sidecar bodies into cached normalized output. |
| T-04-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package install is planned. If implementation attempts any install, stop and add a blocking human verification checkpoint because slopcheck was unavailable. |
</threat_model>

<verification>
Plan-level checks:
- `npm run test -- tests/adapters/gemini-cli/gemini-normalize.test.ts tests/main/core/scanner-cache.test.ts tests/main/ipc/session-view-model-service.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- Gemini evidence becomes valid shared normalized output with stable relationships and diagnostics.
- Cached Gemini records flow through existing core and session view-model services without provider-specific shared code.
- Sidecar bodies remain lazy and allowlist-scoped.
</success_criteria>

<source_coverage_audit>
GOAL: see parsed Gemini sessions flow through the same normalized pipeline as the fake adapter, with parser uncertainty surfaced as diagnostics — COVERED by Task 1.
REQ: ADPT-05 — COVERED by shared-entity mapping for project/session/message/tool/file/shell/output evidence.
REQ: ADPT-06 — COVERED for missing-sidecar, contradiction, and partial-data diagnostics during normalization and lazy artifact loading.
RESEARCH: evidence-only adapters, lazy sidecar loading, scanner/cache/session pipeline reuse — COVERED.
CONTEXT: D-09 through D-14 — COVERED. Full representative fixture corpus and contract/golden regression proof remain for 04-04.
</source_coverage_audit>

<output>
Create `.planning/phases/04-gemini-cli-adapter-end-to-end/04-03-SUMMARY.md` when done.
</output>
