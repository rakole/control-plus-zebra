---
phase: 02-secure-desktop-shell-and-view-model-bridge
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/main/security/content-security-policy.ts
  - src/main/window.ts
  - src/main/electron-main.ts
  - eslint.config.mjs
  - tests/security/browser-window-security.test.ts
  - tests/security/content-security-policy.test.ts
  - tests/security/renderer-forbidden-apis.test.ts
  - tests/boundaries/import-boundaries.test.ts
  - tests/boundaries/fixtures/illegal-renderer-main-import.ts
autonomous: true
requirements:
  - DESK-02
  - DESK-05
  - DESK-06
user_setup: []
must_haves:
  truths:
    - "D-10: renderer security defaults are locked in code and tests: Node integration off, context isolation on, sandbox on, and local-only content loading."
    - "D-11: boundary/security checks for forbidden renderer APIs, preload exposure, and IPC sanitization land in Phase 2 instead of waiting for Phase 8."
    - "D-12: Phase 1 harness-neutrality guardrails extend to renderer and preload without creating adapter-specific exceptions."
    - "DESK-05/DESK-06: renderer cannot read arbitrary local files, run shell commands, import main/adapter internals, or execute remote code."
  artifacts:
    - path: "src/main/security/content-security-policy.ts"
      provides: "Local-only CSP policy builder"
      contains: "default-src 'self'"
    - path: "tests/security/browser-window-security.test.ts"
      provides: "BrowserWindow source assertions for sandbox defaults"
      contains: "nodeIntegration: false"
    - path: "tests/security/renderer-forbidden-apis.test.ts"
      provides: "Renderer forbidden API regression test"
      contains: "child_process"
    - path: "tests/boundaries/import-boundaries.test.ts"
      provides: "Renderer import boundary enforcement"
      contains: "main-process internals"
  key_links:
    - from: "src/main/window.ts"
      to: "src/main/security/content-security-policy.ts"
      via: "CSP applied before renderer content loads"
    - from: "tests/security/renderer-forbidden-apis.test.ts"
      to: "src/renderer/**"
      via: "source scan for forbidden browser/Node/Electron APIs"
    - from: "tests/boundaries/import-boundaries.test.ts"
      to: "src/renderer/**"
      via: "main and adapter import ban"
---

<objective>
Lock the Electron and renderer security proof while the shell surface is still small: add local-only CSP handling, source tests for BrowserWindow defaults, renderer forbidden API checks, and import-boundary enforcement against main/adapter internals.

Purpose: prevent the desktop shell from growing around unsafe Electron defaults or broad renderer privileges.
Output: security helper, hardened window wiring, tests for CSP/window defaults/forbidden APIs, and expanded boundary guardrails.
Locked decisions: D-10, D-11, D-12.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-CONTEXT.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-RESEARCH.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-UI-SPEC.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-VALIDATION.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-PATTERNS.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-01-PLAN.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-02-PLAN.md
@tests/boundaries/import-boundaries.test.ts
@tests/boundaries/shared-naming.test.ts
@eslint.config.mjs
@.spec/spec-from-5.5-revision-1.md
@.spec/additional-instructions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add local-only CSP builder and apply it to renderer responses</name>
  <files>src/main/security/content-security-policy.ts, src/main/window.ts, tests/security/content-security-policy.test.ts</files>
  <read_first>src/main/window.ts, .planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-RESEARCH.md, .planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-UI-SPEC.md</read_first>
  <action>Create `buildContentSecurityPolicy({ mode })` in `src/main/security/content-security-policy.ts`. Production policy must include `default-src 'self'`, `script-src 'self'`, `style-src 'self' 'unsafe-inline'`, `img-src 'self' data:`, `font-src 'self'`, `connect-src 'self'`, `object-src 'none'`, `base-uri 'none'`, and `frame-ancestors 'none'`. Development policy may add only the Vite dev origin needed by the renderer dev server to `connect-src`; do not allow `https:`, `http:` wildcards, remote script sources, or `unsafe-eval`. Apply the CSP through `session.defaultSession.webRequest.onHeadersReceived` or an equivalent Electron main-process response-header hook before loading renderer content. Add tests that assert production policy has no remote script/connect wildcard and development policy names only the local dev origin allowance.</action>
  <verify>npm run test -- tests/security/content-security-policy.test.ts && npm run typecheck</verify>
  <acceptance_criteria>
    - CSP production string contains `default-src 'self'`
    - CSP production string contains `object-src 'none'`, `base-uri 'none'`, and `frame-ancestors 'none'`
    - CSP production string does not contain `https:`, `http:`, or `unsafe-eval`
    - `src/main/window.ts` applies the CSP before renderer content is loaded
  </acceptance_criteria>
  <done>Renderer content is governed by an explicit local-only CSP in development and production paths.</done>
</task>

<task type="auto">
  <name>Task 2: Source-test BrowserWindow security defaults and local loading</name>
  <files>src/main/window.ts, tests/security/browser-window-security.test.ts</files>
  <read_first>src/main/window.ts, src/main/electron-main.ts, .planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-CONTEXT.md</read_first>
  <action>Add `tests/security/browser-window-security.test.ts` to read `src/main/window.ts` and assert the BrowserWindow configuration contains `nodeIntegration: false`, `contextIsolation: true`, `sandbox: true`, and an explicit preload path. Assert renderer loading does not reference `https://`, remote hosts, or user-provided URLs. If `src/main/window.ts` needs refactoring, keep the security values literal enough for the test to detect and keep all window creation inside the main process.</action>
  <verify>npm run test -- tests/security/browser-window-security.test.ts</verify>
  <acceptance_criteria>
    - test fails if `nodeIntegration` is changed to `true`
    - test fails if `contextIsolation` or `sandbox` is removed or disabled
    - test fails if `src/main/window.ts` contains an `https://` renderer load target
    - `src/main/window.ts` still compiles after any refactor
  </acceptance_criteria>
  <done>BrowserWindow security defaults are guarded by an automated regression test.</done>
</task>

<task type="auto">
  <name>Task 3: Block renderer imports from main/adapters and forbidden Node/Electron APIs</name>
  <files>tests/boundaries/import-boundaries.test.ts, tests/boundaries/fixtures/illegal-renderer-main-import.ts, tests/security/renderer-forbidden-apis.test.ts, eslint.config.mjs</files>
  <read_first>tests/boundaries/import-boundaries.test.ts, tests/boundaries/shared-naming.test.ts, eslint.config.mjs, .planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-CONTEXT.md</read_first>
  <action>Extend `tests/boundaries/import-boundaries.test.ts` so renderer files under `src/renderer/**` cannot import `src/main/**` at all, including `src/main/core/**` and `src/main/adapters/**`; renderer code consumes only preload globals and local renderer modules. Add `tests/boundaries/fixtures/illegal-renderer-main-import.ts` as the fixture for a renderer importing a main/core module. Add `tests/security/renderer-forbidden-apis.test.ts` to scan `src/renderer/**/*.ts` and `src/renderer/**/*.tsx` for forbidden imports/usages: `node:fs`, `fs`, `node:child_process`, `child_process`, `electron`, `ipcRenderer`, `window.require`, `require(`, `process.env`, `process.cwd`, `eval(`, and `new Function`. Mirror the same restrictions in `eslint.config.mjs` for renderer files where static ESLint rules can catch them.</action>
  <verify>npm run lint && npm run test:boundaries && npm run test -- tests/security/renderer-forbidden-apis.test.ts</verify>
  <acceptance_criteria>
    - boundary tests reject `src/renderer/**` importing `src/main/core/**`
    - boundary tests continue rejecting `src/renderer/**` importing `src/main/adapters/**`
    - forbidden API test scans `.ts` and `.tsx` renderer files
    - forbidden API test fails on `child_process`, `ipcRenderer`, `window.require`, and `process.cwd`
    - `eslint.config.mjs` includes renderer restrictions for `electron`, `fs`, `child_process`, and generic process/require usage
  </acceptance_criteria>
  <done>Renderer source is guarded from main-process imports, adapter-private imports, and unsafe Node/Electron capabilities.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Boundary: remote or user-controlled content -> renderer process; data crossing: URLs, headers, scripts, styles, and assets
- Boundary: renderer source -> local machine capabilities; data crossing: imports and runtime globals that could reach filesystem, shell, Electron, or main internals
</trust_boundaries>
<threats>
- threat_id: T-02-07
  category: Elevation of Privilege
  component: renderer runtime
  disposition: mitigate
  mitigation_plan: "Keep Node integration disabled, context isolation enabled, sandbox enabled, and source-test those BrowserWindow defaults."
- threat_id: T-02-08
  category: Information Disclosure
  component: renderer imports and globals
  disposition: mitigate
  mitigation_plan: "Ban renderer imports from main/adapters and scan for filesystem, shell, Electron, require, process, eval, and Function usage."
- threat_id: T-02-09
  category: Tampering
  component: remote code loading
  disposition: mitigate
  mitigation_plan: "Apply a local-only CSP and assert renderer load targets are dev-local or packaged-local only."
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test:boundaries` passes
- [ ] `npm run test -- tests/security` passes
- [ ] `rg "https://|unsafe-eval|nodeIntegration: true|contextIsolation: false|sandbox: false" src/main src/renderer tests/security` finds no insecure source configuration
</verification>

<success_criteria>
- All tasks completed
- BrowserWindow security defaults and CSP are explicit and tested
- Renderer source cannot import main-process or adapter-private modules
- Renderer source cannot use filesystem, shell, Electron IPC, generic require/process, eval, or Function APIs
- Security proof remains source/test based without adding Phase 8 packaging smoke scope
</success_criteria>

<output>
After completion, create `.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-03-SUMMARY.md`
</output>

