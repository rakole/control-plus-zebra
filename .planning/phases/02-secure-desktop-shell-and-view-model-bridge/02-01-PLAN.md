---
phase: 02-secure-desktop-shell-and-view-model-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - forge.config.ts
  - vite.main.config.ts
  - vite.preload.config.ts
  - vite.renderer.config.ts
  - tsconfig.json
  - vitest.config.ts
  - src/main/electron-main.ts
  - src/main/window.ts
  - src/preload/index.ts
  - src/preload/types.ts
  - src/renderer/main.tsx
  - src/renderer/App.tsx
  - src/renderer/styles.css
  - src/renderer/test/setup.ts
autonomous: true
requirements:
  - DESK-01
  - DESK-02
  - DESK-06
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: Electron Forge + Vite + React + TypeScript is added surgically around the existing repo, preserving Phase 1 shared-core ownership and producing a launchable local desktop shell."
    - "D-03: renderer routing is available from the first scaffold slice so later Overview, Projects, Sessions, Session Detail, and Diagnostics routes do not require a routing rewrite."
    - "D-10: the initial BrowserWindow path is local-only and explicitly uses `nodeIntegration: false`, `contextIsolation: true`, and `sandbox: true`."
    - "DESK-01/DESK-06: the app can launch local packaged/development content without remote-code loading or V1 control surfaces."
  artifacts:
    - path: "package.json"
      provides: "Electron Forge/Vite/React scripts and dependency baseline"
      contains: "\"start\""
    - path: "forge.config.ts"
      provides: "Electron Forge entrypoint configuration"
      contains: "@electron-forge/plugin-vite"
    - path: "src/main/electron-main.ts"
      provides: "Electron app lifecycle entrypoint"
      contains: "app.whenReady"
    - path: "src/main/window.ts"
      provides: "BrowserWindow construction seam"
      contains: "nodeIntegration: false"
    - path: "src/renderer/App.tsx"
      provides: "Initial routed React shell"
      contains: "Sessions"
  key_links:
    - from: "src/main/electron-main.ts"
      to: "src/main/window.ts"
      via: "window creation"
    - from: "src/preload/index.ts"
      to: "src/preload/types.ts"
      via: "typed bridge declaration"
    - from: "src/renderer/main.tsx"
      to: "src/renderer/App.tsx"
      via: "React root rendering"
---

<objective>
Scaffold the secure desktop shell foundation: add Electron Forge with Vite, React, TypeScript, renderer routing, and a minimal local app window while preserving the Phase 1 contract/test/lint baseline.

Purpose: give Phase 2 a launchable shell and test harness before IPC/view-model and UI depth are added.
Output: Electron/Vite config, npm scripts, main/preload/renderer entrypoints, renderer test setup, and a routed shell frame that defaults to `/sessions`.
Locked decisions: D-01, D-02, D-03, D-10.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-CONTEXT.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-RESEARCH.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-UI-SPEC.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-VALIDATION.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-PATTERNS.md
@.planning/phases/01-architecture-contracts-and-fixture-proof/01-CONTEXT.md
@.spec/spec-from-5.5-revision-1.md
@.spec/additional-instructions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Electron Forge, Vite, React, routing, and renderer test dependencies</name>
  <files>package.json, package-lock.json, forge.config.ts, vite.main.config.ts, vite.preload.config.ts, vite.renderer.config.ts, tsconfig.json, vitest.config.ts, src/renderer/test/setup.ts</files>
  <read_first>package.json, package-lock.json, tsconfig.json, vitest.config.ts, eslint.config.mjs, .planning/research/STACK.md, .planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-UI-SPEC.md</read_first>
  <action>Add Electron 42.2.0, Electron Forge 7.11.2 with the Vite plugin, Vite 8.0.14, React 19.2.6, `react-dom`, `react-router`, `lucide-react`, and renderer test dependencies (`jsdom`, `@testing-library/react`, `@testing-library/user-event`, `@testing-library/jest-dom`). Add npm scripts `start`, `package`, `make`, and `test:renderer` while preserving `lint`, `typecheck`, `test`, and `test:boundaries`. Configure Forge to use Vite entrypoints for main, preload, and renderer. Configure Vitest so existing Node tests stay in the Node environment and renderer tests under `tests/renderer/**/*.test.tsx` run in jsdom with `src/renderer/test/setup.ts`.</action>
  <verify>npm run typecheck && npm run test:boundaries</verify>
  <acceptance_criteria>
    - `package.json` still contains scripts named `lint`, `typecheck`, `test`, and `test:boundaries`
    - `package.json` contains scripts named `start`, `package`, `make`, and `test:renderer`
    - `forge.config.ts` contains `@electron-forge/plugin-vite`
    - `vite.main.config.ts`, `vite.preload.config.ts`, and `vite.renderer.config.ts` exist
    - `vitest.config.ts` includes a jsdom renderer test target without moving existing boundary tests out of Node
  </acceptance_criteria>
  <done>Tooling can typecheck and the existing Phase 1 boundary suite still runs with the new Electron/Vite config present.</done>
</task>

<task type="auto">
  <name>Task 2: Create the local Electron main/window and preload entrypoints</name>
  <files>src/main/electron-main.ts, src/main/window.ts, src/preload/index.ts, src/preload/types.ts</files>
  <read_first>src/main/core/registry/register-bundled-adapters.ts, .planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-CONTEXT.md, .planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-RESEARCH.md, .spec/spec-from-5.5-revision-1.md</read_first>
  <action>Create `src/main/electron-main.ts` to initialize the app with `app.whenReady()`, create one window, handle macOS activate behavior, and quit on non-macOS all-windows-closed. Create `src/main/window.ts` with a `createMainWindow()` function that constructs `BrowserWindow` using `nodeIntegration: false`, `contextIsolation: true`, `sandbox: true`, and `preload` pointing at the Vite preload bundle. Load only Vite dev-server content in development and the local generated renderer HTML in packaged/production mode. Create `src/preload/types.ts` and `src/preload/index.ts` with an empty typed `agentWorkbench` bridge declaration; do not expose `ipcRenderer`, `fs`, `child_process`, Electron, or a generic invoke helper.</action>
  <verify>npm run typecheck && rg "nodeIntegration: false|contextIsolation: true|sandbox: true" src/main/window.ts</verify>
  <acceptance_criteria>
    - `src/main/electron-main.ts` contains `app.whenReady`
    - `src/main/window.ts` contains `nodeIntegration: false`, `contextIsolation: true`, and `sandbox: true`
    - `src/main/window.ts` does not call `loadURL` with an `https://` or other remote URL
    - `src/preload/index.ts` does not export or expose `ipcRenderer`
    - `src/preload/types.ts` declares the future `window.agentWorkbench` type surface
  </acceptance_criteria>
  <done>The app has a secure local main/preload skeleton that compiles and is ready for typed IPC methods.</done>
</task>

<task type="auto">
  <name>Task 3: Render the initial routed React shell and initialize approved UI tooling</name>
  <files>src/renderer/main.tsx, src/renderer/App.tsx, src/renderer/styles.css, package.json, package-lock.json, components.json</files>
  <read_first>.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-UI-SPEC.md, src/renderer/main.tsx, src/renderer/App.tsx, src/renderer/styles.css, package.json</read_first>
  <action>Create `src/renderer/main.tsx` and `src/renderer/App.tsx` with React Router declarative routing. Default to `/sessions`, render persistent workbench chrome with Overview, Projects, Sessions, and Diagnostics nav labels, mark Sessions active, and keep non-Sessions entries disabled with muted styling. Apply the UI-SPEC typography, color, spacing, sidebar width 224px, header height 56px, and route canvas basics in `src/renderer/styles.css`. After the React/Vite shell exists, run `npx shadcn@latest init --preset b2Ciqm1BK` and keep only official shadcn configuration output; do not add third-party registries or blocks.</action>
  <verify>npm run typecheck && npm run test:renderer</verify>
  <acceptance_criteria>
    - `src/renderer/App.tsx` uses React Router and has a `/sessions` route
    - `src/renderer/App.tsx` renders the fixed labels `Overview`, `Projects`, `Sessions`, and `Diagnostics`
    - disabled nav entries use `aria-disabled="true"`
    - `src/renderer/styles.css` contains `#F8FAFC`, `#FFFFFF`, `#2563EB`, `224px`, and `56px`
    - `components.json` exists and records the approved shadcn preset without third-party registries
  </acceptance_criteria>
  <done>The first local shell route renders with approved visual tokens and officially initialized UI tooling.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Boundary: Electron main process -> renderer process; data crossing: local app HTML, preload script, and renderer assets
- Boundary: developer dependencies/config -> executable desktop shell; data crossing: package scripts, Forge config, Vite config, and generated bundles
</trust_boundaries>
<threats>
- threat_id: T-02-01
  category: Elevation of Privilege
  component: BrowserWindow webPreferences
  disposition: mitigate
  mitigation_plan: "Set `nodeIntegration: false`, `contextIsolation: true`, and `sandbox: true` in the first BrowserWindow construction task and verify those strings in source."
- threat_id: T-02-02
  category: Tampering
  component: scaffold integration
  disposition: mitigate
  mitigation_plan: "Add Forge/Vite config additively and keep Phase 1 scripts, typecheck, lint, and boundary tests intact."
- threat_id: T-02-03
  category: Information Disclosure
  component: preload bridge declaration
  disposition: mitigate
  mitigation_plan: "Expose only an empty typed product bridge in this plan and defer concrete methods to the validated IPC plan."
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm run test:boundaries` passes
- [ ] `npm run test:renderer` passes
- [ ] `rg "nodeIntegration: false|contextIsolation: true|sandbox: true" src/main/window.ts` finds all three security defaults
- [ ] `rg "ipcRenderer|child_process|from \"node:fs\"|from 'node:fs'" src/renderer src/preload` returns no unsafe bridge/renderer exposure
</verification>

<success_criteria>
- All tasks completed
- Electron Forge + Vite + React + TypeScript scaffold is present and launchable through `npm run start`
- Renderer routing defaults to Sessions without implementing later pages
- Existing Phase 1 contracts and boundary tests still pass
- BrowserWindow and preload skeleton do not expose unsafe renderer capabilities
</success_criteria>

<output>
After completion, create `.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-01-SUMMARY.md`
</output>
