---
phase: 02-secure-desktop-shell-and-view-model-bridge
plan: 04
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - src/renderer/App.tsx
  - src/renderer/routes/SessionsRoute.tsx
  - src/renderer/components/AppShell.tsx
  - src/renderer/components/SessionList.tsx
  - src/renderer/components/SessionPreview.tsx
  - src/renderer/components/CapabilityBadge.tsx
  - src/renderer/components/LoadingSkeleton.tsx
  - src/renderer/styles.css
  - tests/renderer/sessions-route.test.tsx
  - tests/renderer/renderer-boundary-source.test.ts
autonomous: true
requirements:
  - DESK-01
  - DESK-03
  - DESK-04
  - DESK-05
user_setup:
  - "Manual launch check remains required because full Electron smoke automation is deferred to Phase 8: run the Phase 2 dev command and confirm one local window opens to the Sessions route."
must_haves:
  truths:
    - "D-07/D-08: the first real renderer route is Sessions-first with list/detail preview, status/capability cues, and no full timeline or Run Audit scope."
    - "D-09: session data loads through `window.agentWorkbench` and main-process fake-adapter-backed DTOs, not renderer-local mocks."
    - "UI-SPEC: the shell is a quiet local desktop tool with 224px sidebar, 56px header, dense rows, selected-session preview, explicit unsupported/unknown badges, and no marketing hero."
    - "V1 read-only scope: the UI exposes `Reload Sessions` only; there are no launch, approve, reject, terminal, PR, cleanup, delete, reset, or source-mutation controls."
  artifacts:
    - path: "src/renderer/routes/SessionsRoute.tsx"
      provides: "Sessions-first route"
      contains: "Reload Sessions"
    - path: "src/renderer/components/SessionPreview.tsx"
      provides: "Selected session summary preview"
      contains: "Select a session to inspect its summary."
    - path: "src/renderer/components/CapabilityBadge.tsx"
      provides: "Unsupported/Unknown capability label rendering"
      contains: "Unsupported"
    - path: "tests/renderer/sessions-route.test.tsx"
      provides: "Renderer behavior and copy tests"
      contains: "Reload Sessions"
  key_links:
    - from: "src/renderer/routes/SessionsRoute.tsx"
      to: "src/preload/types.ts"
      via: "window.agentWorkbench list/get methods"
    - from: "src/renderer/components/SessionList.tsx"
      to: "src/renderer/components/SessionPreview.tsx"
      via: "selected session id and preview update"
    - from: "tests/renderer/sessions-route.test.tsx"
      to: "src/renderer/routes/SessionsRoute.tsx"
      via: "loading, selection, reload, error, and unsupported/unknown rendering"
---

<objective>
Build the first real Phase 2 app experience: a Sessions-first workbench route that consumes sanitized preload DTOs, supports list/detail preview selection, shows capability truth states explicitly, and stays inside the approved local read-only desktop UI contract.

Purpose: prove the main/preload/renderer seam from a real React route without adding later-phase dashboard, timeline, audit, source-scanning, or control surfaces.
Output: renderer shell components, Sessions route, styles, and renderer tests for loading, selection, reload, error, and unsupported/unknown states.
Locked decisions: D-02, D-03, D-07, D-08, D-09, D-12.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-CONTEXT.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-RESEARCH.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-UI-SPEC.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-VALIDATION.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-PATTERNS.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-01-PLAN.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-02-PLAN.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-03-PLAN.md
@src/preload/types.ts
@src/main/ipc/view-models.ts
@.spec/spec-from-5.5-revision-1.md
@.spec/additional-instructions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build reusable renderer shell, badges, list, preview, and loading components</name>
  <files>src/renderer/components/AppShell.tsx, src/renderer/components/CapabilityBadge.tsx, src/renderer/components/SessionList.tsx, src/renderer/components/SessionPreview.tsx, src/renderer/components/LoadingSkeleton.tsx, src/renderer/styles.css</files>
  <read_first>.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-UI-SPEC.md, src/renderer/App.tsx, src/preload/types.ts, src/main/ipc/view-models.ts</read_first>
  <action>Create renderer-only components for the approved shell. `AppShell` renders a persistent 224px sidebar, 56px header, active `Sessions` nav item, disabled nav entries for `Overview`, `Projects`, and `Diagnostics` with `aria-disabled="true"` and title `Available in a later phase`. `CapabilityBadge` renders exactly `Supported`, `Unsupported`, or `Unknown`, with unsupported/unknown using neutral outlined styling and no green success treatment. `SessionList` renders dense selectable rows with at least 64px hit height, adapter display name, lifecycle status, timestamps, and capability warning summary. `SessionPreview` renders selected session summary, harness badge, lifecycle/status label, timestamps, capability warnings, diagnostic count, and evidence summary counts only; it must not render full timeline or Run Audit sections. `LoadingSkeleton` renders three compact row skeletons and one preview block. Keep colors, typography, spacing, selected-row fill `#EFF6FF`, and responsive stacking below 900px aligned to the UI-SPEC.</action>
  <verify>npm run typecheck && npm run test:renderer</verify>
  <acceptance_criteria>
    - `AppShell` renders `Overview`, `Projects`, `Sessions`, and `Diagnostics`
    - disabled nav entries include `aria-disabled="true"` and `Available in a later phase`
    - `CapabilityBadge` contains exact labels `Supported`, `Unsupported`, and `Unknown`
    - `SessionPreview` contains `Select a session to inspect its summary.`
    - `src/renderer/styles.css` contains `#EFF6FF`, `#F1F5F9`, `min-height: 64px`, and a media query for `900px`
  </acceptance_criteria>
  <done>Renderer component primitives match the approved shell contract and compile against preload DTO types.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Sessions route data loading, selection, keyboard navigation, reload, and errors</name>
  <files>src/renderer/App.tsx, src/renderer/routes/SessionsRoute.tsx, tests/renderer/sessions-route.test.tsx</files>
  <read_first>src/renderer/App.tsx, src/renderer/components/AppShell.tsx, src/renderer/components/SessionList.tsx, src/renderer/components/SessionPreview.tsx, src/preload/types.ts, .planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-UI-SPEC.md</read_first>
  <action>Wire `/sessions` to `SessionsRoute`. On initial render, call `window.agentWorkbench.listSessions({ limit: 50 })`, show the loading skeleton while pending, auto-select the first returned session, and call `window.agentWorkbench.getSessionById({ sessionId })` for the selected preview. Clicking a row selects it and updates the preview. ArrowDown/ArrowUp moves focus through rows and Enter selects the focused row. The primary CTA text is exactly `Reload Sessions` and reruns the read-only list request through preload; it must not scan arbitrary roots or mutate data. Empty state text must match `No sessions available` and `The desktop shell is running, but the bridge returned no session summaries. Reload sessions after the fake-adapter view model is available.` Error state text must match `Sessions could not load. Check the preload bridge and IPC handler, then reload sessions.` Sanitized errors only: do not render stack traces, raw filesystem paths, raw adapter-private names, or shell command details.</action>
  <verify>npm run test -- tests/renderer/sessions-route.test.tsx && npm run typecheck</verify>
  <acceptance_criteria>
    - route calls `window.agentWorkbench.listSessions` on initial render
    - route calls `window.agentWorkbench.getSessionById` when a session is selected
    - clicking a second row updates preview content
    - ArrowDown followed by Enter selects the next focused row
    - `Reload Sessions` triggers another `listSessions` call and no other mutation/control API
    - empty and error copy exactly match the UI-SPEC strings
  </acceptance_criteria>
  <done>Sessions route behaves like a real read-only bridge-backed workbench route.</done>
</task>

<task type="auto">
  <name>Task 3: Prove renderer boundary, capability truth, and read-only scope</name>
  <files>tests/renderer/sessions-route.test.tsx, tests/renderer/renderer-boundary-source.test.ts, src/renderer/routes/SessionsRoute.tsx, src/renderer/components/CapabilityBadge.tsx</files>
  <read_first>tests/security/renderer-forbidden-apis.test.ts, tests/boundaries/import-boundaries.test.ts, src/renderer/routes/SessionsRoute.tsx, src/renderer/components/CapabilityBadge.tsx, .planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-UI-SPEC.md</read_first>
  <action>Add renderer tests that mock `window.agentWorkbench` and provide DTOs with unsupported and unknown capability badges. Assert `Unsupported` and `Unknown` render as labels and are not replaced by `0`, `Passed`, `Clean`, or hidden values. Add `tests/renderer/renderer-boundary-source.test.ts` to scan renderer source and fail if forbidden read-only-scope controls or text appear: `Launch`, `Approve`, `Reject`, `Terminal`, `Create PR`, `Cleanup`, `Delete`, `Reset`, `Run command`, `shell`, or `exec`. Also assert renderer source does not import from `src/main/` or `src/main/adapters/`. Keep all provider display copy driven by DTO metadata; no renderer branch may compare `adapterId` to `gemini-cli` or hard-code Gemini-specific names.</action>
  <verify>npm run test:renderer && npm run test:boundaries && npm run test -- tests/security/renderer-forbidden-apis.test.ts</verify>
  <acceptance_criteria>
    - renderer test fixture with unknown/unsupported capabilities renders `Unknown` and `Unsupported`
    - renderer tests fail if unknown/unsupported states are rendered as `0`, `Passed`, or `Clean`
    - `tests/renderer/renderer-boundary-source.test.ts` fails if renderer source contains launch/approve/reject/terminal/PR/cleanup/delete/reset/run-command controls
    - renderer source contains no `adapterId === "gemini-cli"` or `Gemini` branching/copy except data supplied by test DTOs
  </acceptance_criteria>
  <done>Renderer behavior proves capability truth and V1 read-only scope at the source/test level.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Manual local launch check</name>
  <files>package.json, src/main/electron-main.ts, src/renderer/App.tsx</files>
  <read_first>.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-VALIDATION.md, package.json</read_first>
  <action>Run the Phase 2 development app command from `package.json` and verify one local Electron window opens to the Sessions route. Confirm the shell shows the persistent sidebar/header, the `Reload Sessions` control, session rows from the fake-backed bridge, and a selected-session preview. Confirm there are no controls for launch, approve, reject, terminal input, PR creation, cleanup, delete, reset, or arbitrary source mutation.</action>
  <verify>manual: one local Electron window opens to Sessions and matches the read-only UI contract</verify>
  <acceptance_criteria>
    - one local app window opens
    - default route title is `Sessions`
    - `Reload Sessions` is visible
    - selected-session preview is visible after fake DTOs load
    - no forbidden V1 mutation/control UI is visible
  </acceptance_criteria>
  <done>Developer confirms the local shell launches and the first route matches the Phase 2 UI contract.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Boundary: preload DTOs -> renderer state; data crossing: shell state, session list, selected preview, capability labels, counts, and sanitized errors
- Boundary: user interaction -> read-only preload methods; data crossing: row selection, keyboard navigation, and reload request
</trust_boundaries>
<threats>
- threat_id: T-02-10
  category: Information Disclosure
  component: renderer error and preview surfaces
  disposition: mitigate
  mitigation_plan: "Render only sanitized DTO fields and UI-SPEC error copy; do not expose stack traces, raw paths, raw transcripts, adapter-private names, or shell details."
- threat_id: T-02-11
  category: Tampering
  component: read-only UI controls
  disposition: mitigate
  mitigation_plan: "Limit renderer actions to session selection and `Reload Sessions`; source-test for forbidden V1 mutation/control labels."
- threat_id: T-02-12
  category: Spoofing
  component: capability status rendering
  disposition: mitigate
  mitigation_plan: "Render unsupported and unknown capability states exactly as `Unsupported` and `Unknown`, never as success or zero values."
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test` passes
- [ ] `npm run test:boundaries` passes
- [ ] `npm run test:renderer` passes
- [ ] manual local Electron launch check passes
</verification>

<success_criteria>
- All tasks completed
- `/sessions` is the default and only real route
- Session rows and preview data load through `window.agentWorkbench`
- Unsupported and unknown capability states render explicitly
- UI matches the approved desktop workbench contract and remains read-only
- Renderer source respects boundary/security tests from plans `02-02` and `02-03`
</success_criteria>

<output>
After completion, create `.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-04-SUMMARY.md`
</output>
