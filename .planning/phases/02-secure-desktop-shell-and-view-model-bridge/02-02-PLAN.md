---
phase: 02-secure-desktop-shell-and-view-model-bridge
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/main/app/session-view-model-service.ts
  - src/main/ipc/channels.ts
  - src/main/ipc/view-models.ts
  - src/main/ipc/handlers.ts
  - src/main/ipc/index.ts
  - src/main/electron-main.ts
  - src/preload/types.ts
  - src/preload/index.ts
  - tests/main/ipc/session-view-model-service.test.ts
  - tests/main/ipc/ipc-handlers.test.ts
  - tests/preload/preload-api-surface.test.ts
autonomous: true
requirements:
  - DESK-03
  - DESK-04
  - DESK-05
user_setup: []
must_haves:
  truths:
    - "D-04: preload exposes one typed method per allowed operation and never exposes `ipcRenderer`, generic `invoke`, or broad Electron APIs."
    - "D-05: IPC channels are small, future-shaped, and harness-neutral: `app:getShellState`, `sessions:list`, and `sessions:getById`."
    - "D-06/D-09: IPC handlers validate input and return sanitized view models derived from existing shared-core and fake-adapter proof surfaces, not renderer-local mocks or adapter-private objects."
    - "DESK-03/DESK-04: renderer-facing DTOs are intentionally smaller than normalized core entities and preserve unsupported/unknown capability truth."
  artifacts:
    - path: "src/main/ipc/channels.ts"
      provides: "Allowed IPC channel constants"
      contains: "sessions:list"
    - path: "src/main/ipc/view-models.ts"
      provides: "Zod schemas and DTO types for renderer-safe IPC payloads"
      contains: "SessionSummaryViewModel"
    - path: "src/preload/types.ts"
      provides: "Typed renderer bridge contract"
      contains: "listSessions"
    - path: "tests/preload/preload-api-surface.test.ts"
      provides: "Preload surface leak regression test"
      contains: "ipcRenderer"
  key_links:
    - from: "src/main/app/session-view-model-service.ts"
      to: "src/main/core/registry/register-bundled-adapters.ts"
      via: "fake adapter proof data through bundled registry"
    - from: "src/main/ipc/handlers.ts"
      to: "src/main/ipc/view-models.ts"
      via: "validated request and response schemas"
    - from: "src/preload/index.ts"
      to: "src/preload/types.ts"
      via: "one method per allowed IPC operation"
---

<objective>
Implement the typed preload and IPC view-model bridge for Phase 2: define harness-neutral channels, validate payloads, map fake-adapter normalized data into sanitized session summary DTOs, and expose one narrow preload method per operation.

Purpose: prove the renderer gets safe view models through main/preload IPC rather than raw files, raw core objects, adapter-private objects, or renderer-local mocks.
Output: app-shell view-model service, Zod-backed IPC schemas/handlers, preload API types, and unit tests for validation and bridge surface restrictions.
Locked decisions: D-04, D-05, D-06, D-07, D-08, D-09.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-CONTEXT.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-RESEARCH.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-UI-SPEC.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-VALIDATION.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-PATTERNS.md
@.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-01-PLAN.md
@src/main/core/registry/register-bundled-adapters.ts
@src/main/core/adapter-contract/session-source-adapter.ts
@src/main/adapters/fake-test/fixtures/phase1-session.fixture.json
@tests/adapters/fake-test/fake-adapter.smoke.test.ts
@.spec/spec-from-5.5-revision-1.md
@.spec/additional-instructions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define harness-neutral IPC channels and sanitized view-model schemas</name>
  <files>src/main/ipc/channels.ts, src/main/ipc/view-models.ts, src/main/ipc/index.ts</files>
  <read_first>.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-CONTEXT.md, .planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-UI-SPEC.md, src/main/core/model/entities.ts, src/main/core/model/capabilities.ts</read_first>
  <action>Create `src/main/ipc/channels.ts` with exactly these allowed channel constants: `app:getShellState`, `sessions:list`, and `sessions:getById`. Create `src/main/ipc/view-models.ts` with Zod schemas and TypeScript types for `CapabilityBadgeViewModel`, `SessionSummaryViewModel`, `SessionPreviewViewModel`, `ShellStateViewModel`, `ListSessionsRequest`, `ListSessionsResponse`, `GetSessionByIdRequest`, and `GetSessionByIdResponse`. DTOs must include `adapterId`, adapter display name, `sessionId`, optional native session id, title, lifecycle status, timestamps, capability badges with `Supported | Unsupported | Unknown` labels, diagnostic warning count, evidence summary counts, and sanitized error shape. Do not export raw `Session`, `SessionEvent`, `ToolCall`, `ShellCommandEvidence`, `OutputArtifact`, or adapter-private types from the view-model module.</action>
  <verify>npm run typecheck && rg "app:getShellState|sessions:list|sessions:getById" src/main/ipc/channels.ts</verify>
  <acceptance_criteria>
    - `src/main/ipc/channels.ts` contains `app:getShellState`, `sessions:list`, and `sessions:getById`
    - `src/main/ipc/view-models.ts` exports Zod schemas for list and get-by-id request/response payloads
    - `src/main/ipc/view-models.ts` contains the labels `Supported`, `Unsupported`, and `Unknown`
    - `src/main/ipc/view-models.ts` does not export adapter-private raw event types
  </acceptance_criteria>
  <done>IPC channel and DTO contracts are explicit, harness-neutral, and validated by Zod schemas.</done>
</task>

<task type="auto">
  <name>Task 2: Map fake-adapter proof data into renderer-safe session view models</name>
  <files>src/main/app/session-view-model-service.ts, tests/main/ipc/session-view-model-service.test.ts</files>
  <read_first>src/main/core/registry/register-bundled-adapters.ts, src/main/adapters/fake-test/fixtures/phase1-session.fixture.json, tests/adapters/fake-test/fake-adapter.smoke.test.ts, src/main/ipc/view-models.ts</read_first>
  <action>Create `src/main/app/session-view-model-service.ts` with `createSessionViewModelService()` that uses `createBundledAdapterRegistry()` to require `fake-test`, validates/discovers/parses/normalizes the existing `src/main/adapters/fake-test/fixtures/phase1-session.fixture.json`, and maps normalized output into `SessionSummaryViewModel` and `SessionPreviewViewModel` objects. Preserve adapter display labels from descriptor metadata. Map capability statuses exactly to `Supported`, `Unsupported`, and `Unknown`; never convert unknown/unsupported to numeric zero, `Passed`, or `Clean`. Include evidence counts for messages, tool calls, shell commands, output artifacts, file mutations, and diagnostics, but do not return raw message content, raw artifact paths, raw filesystem records, or adapter-private payloads. Add tests proving the service returns at least one fake-backed session, includes `Fake Test Harness`, includes explicit `Unsupported` and `Unknown` badges from fixture capabilities, and does not expose fields named `rawEvents`, `artifactPath`, `verificationStatus`, `runAuditStatus`, or `attentionReasons`.</action>
  <verify>npm run test -- tests/main/ipc/session-view-model-service.test.ts</verify>
  <acceptance_criteria>
    - service output contains at least one session summary from the fake fixture
    - service output includes adapter display name `Fake Test Harness`
    - service output includes capability labels `Unsupported` and `Unknown`
    - service output does not contain keys `rawEvents`, `artifactPath`, `verificationStatus`, `runAuditStatus`, or `attentionReasons`
  </acceptance_criteria>
  <done>Fake adapter data flows through main-process mapping into sanitized session DTOs.</done>
</task>

<task type="auto">
  <name>Task 3: Register IPC handlers with request validation and sanitized errors</name>
  <files>src/main/ipc/handlers.ts, src/main/ipc/index.ts, src/main/electron-main.ts, tests/main/ipc/ipc-handlers.test.ts</files>
  <read_first>src/main/ipc/channels.ts, src/main/ipc/view-models.ts, src/main/app/session-view-model-service.ts, src/main/electron-main.ts</read_first>
  <action>Create `registerIpcHandlers(ipcMain, service)` in `src/main/ipc/handlers.ts`. Register handlers only for the three allowed channels. Validate every incoming payload with the matching Zod request schema. Return `ShellStateViewModel`, `ListSessionsResponse`, or `GetSessionByIdResponse` after validating outgoing DTOs with response schemas. Convert thrown errors and Zod failures into sanitized user-facing errors with code strings such as `invalid-request` and `session-load-failed`; do not return stack traces, raw filesystem paths, raw adapter objects, or raw Electron errors. Wire `registerIpcHandlers` from `src/main/electron-main.ts` before window creation. Add handler tests using a fake `ipcMain.handle` collector to prove only the three allowed channels register, invalid `sessions:getById` payloads return a sanitized error, and valid list/get calls return schema-valid DTOs.</action>
  <verify>npm run test -- tests/main/ipc/ipc-handlers.test.ts</verify>
  <acceptance_criteria>
    - `src/main/ipc/handlers.ts` registers exactly `app:getShellState`, `sessions:list`, and `sessions:getById`
    - invalid `sessions:getById` input returns an error with code `invalid-request`
    - error responses do not include `stack`, raw filesystem paths, or adapter-private object names
    - valid handler responses pass their response Zod schemas
  </acceptance_criteria>
  <done>Main-process IPC handlers validate requests, validate responses, and expose sanitized failures.</done>
</task>

<task type="auto">
  <name>Task 4: Expose one typed preload method per allowed operation</name>
  <files>src/preload/types.ts, src/preload/index.ts, tests/preload/preload-api-surface.test.ts</files>
  <read_first>src/preload/types.ts, src/preload/index.ts, src/main/ipc/channels.ts, src/main/ipc/view-models.ts, .planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-CONTEXT.md</read_first>
  <action>Update `src/preload/types.ts` so `window.agentWorkbench` exposes exactly `getShellState()`, `listSessions(request?)`, and `getSessionById(request)`. Update `src/preload/index.ts` to use `contextBridge.exposeInMainWorld("agentWorkbench", api)` and one private `ipcRenderer.invoke` call per method. Do not expose `ipcRenderer`, `invoke`, `send`, `on`, `removeListener`, `fs`, `child_process`, `shell`, or any broad Electron namespace. Add a source-level preload test that reads `src/preload/index.ts` and `src/preload/types.ts` and asserts the public bridge contains the three named methods and does not expose forbidden API names outside private imports/usages needed to implement the bridge.</action>
  <verify>npm run test -- tests/preload/preload-api-surface.test.ts && npm run typecheck</verify>
  <acceptance_criteria>
    - `src/preload/types.ts` declares `getShellState`, `listSessions`, and `getSessionById`
    - `src/preload/index.ts` contains `contextBridge.exposeInMainWorld("agentWorkbench"`
    - no public preload type exposes a method named `invoke`, `send`, `on`, or `ipcRenderer`
    - `tests/preload/preload-api-surface.test.ts` fails on generic bridge helper exposure
  </acceptance_criteria>
  <done>The renderer has a narrow typed bridge with one method per allowed operation and no generic IPC escape hatch.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Boundary: renderer process -> main process IPC; data crossing: request payloads for shell state, session list, and selected session preview
- Boundary: adapter/core normalized data -> renderer DTOs; data crossing: sanitized summaries, capability labels, counts, and errors
</trust_boundaries>
<threats>
- threat_id: T-02-04
  category: Elevation of Privilege
  component: preload bridge
  disposition: mitigate
  mitigation_plan: "Expose only `getShellState`, `listSessions`, and `getSessionById`; keep `ipcRenderer` and generic invoke helpers private and source-tested."
- threat_id: T-02-05
  category: Information Disclosure
  component: session view-model mapping
  disposition: mitigate
  mitigation_plan: "Return sanitized DTOs with counts and labels instead of raw transcript content, raw paths, adapter-private payloads, or stack traces."
- threat_id: T-02-06
  category: Tampering
  component: IPC request payloads
  disposition: mitigate
  mitigation_plan: "Validate every incoming IPC request with Zod before service access and return sanitized `invalid-request` errors."
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm run test -- tests/main/ipc/session-view-model-service.test.ts` passes
- [ ] `npm run test -- tests/main/ipc/ipc-handlers.test.ts` passes
- [ ] `npm run test -- tests/preload/preload-api-surface.test.ts` passes
- [ ] `rg "gemini-cli|Gemini" src/main/ipc src/main/app src/preload` returns no shared/provider-specific branch or type leaks
</verification>

<success_criteria>
- All tasks completed
- Preload exposes exactly one typed method per allowed IPC operation
- IPC handlers validate input and response DTOs
- Renderer-facing session view models come from fake-adapter data through main process code
- Unsupported and unknown capability states survive into DTOs as explicit labels
- No raw adapter-private objects, raw filesystem records, stack traces, or final audit/verification conclusions cross the IPC boundary
</success_criteria>

<output>
After completion, create `.planning/phases/02-secure-desktop-shell-and-view-model-bridge/02-02-SUMMARY.md`
</output>

