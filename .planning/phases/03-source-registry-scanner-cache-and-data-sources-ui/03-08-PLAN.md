---
phase: 03-source-registry-scanner-cache-and-data-sources-ui
plan: 08
type: execute
wave: 6
depends_on:
  - 03-07
files_modified:
  - src/renderer/App.tsx
  - src/renderer/components/AppShell.tsx
  - src/renderer/components/DataSourceDetail.tsx
  - src/renderer/components/DataSourceList.tsx
  - src/renderer/components/DataSourcesLoadingSkeleton.tsx
  - src/renderer/components/SourceStatusBadge.tsx
  - src/renderer/routes/DataSourcesRoute.tsx
  - src/renderer/styles.css
  - tests/renderer/data-sources-route.test.tsx
autonomous: true
requirements:
  - UI-06
must_haves:
  truths:
    - "User can manage harnesses and data sources from a Harnesses and Data Sources settings page per UI-06."
    - "Data Sources UI uses split list/detail management surface per D-01, with scan-friendly list fields per D-02 and detail-owned add/edit/validate/rescan interactions per D-03."
    - "Adding or editing validates first, preserves failed entries with diagnostics, and never scans automatically per D-04 and D-05."
    - "UI renders cache, validation, scan, watcher, capability, stale, unsupported, and unknown states explicitly per D-07, D-08, D-13, and D-15."
    - "Typed path entry is used; no native picker, delete source, reset source, clear cache, shell execution, or live watcher controls are added per D-10."
  artifacts:
    - path: "src/renderer/routes/DataSourcesRoute.tsx"
      provides: "Harnesses and Data Sources settings route"
    - path: "src/renderer/components/DataSourceList.tsx"
      provides: "Scan-friendly source list"
    - path: "src/renderer/components/DataSourceDetail.tsx"
      provides: "Add/edit/validate/scan detail panel"
    - path: "tests/renderer/data-sources-route.test.tsx"
      provides: "Renderer UI behavior proof"
  key_links:
    - from: "src/renderer/routes/DataSourcesRoute.tsx"
      to: "src/preload/types.ts"
      via: "window.agentWorkbench named methods"
      pattern: "window\\.agentWorkbench"
    - from: "src/renderer/App.tsx"
      to: "src/renderer/routes/DataSourcesRoute.tsx"
      via: "HashRouter route registration"
      pattern: "data-sources"
---

<objective>
Build the `/data-sources` renderer route and split list/detail Data Sources UI using the main/preload contract from 03-07.

Purpose: Users need a quiet local settings workflow to add, enable/disable, validate, explicitly scan/rescan, and inspect source/cache diagnostics without renderer filesystem access or provider-specific branches.
Output: Data Sources route, renderer components/styles, and focused renderer tests.
</objective>

<execution_context>
@/Users/rhishi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rhishi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-CONTEXT.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-RESEARCH.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-UI-SPEC.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-PATTERNS.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-07-SUMMARY.md
@src/renderer/App.tsx
@src/renderer/components/AppShell.tsx
@src/renderer/routes/SessionsRoute.tsx
@src/renderer/styles.css
@tests/renderer/sessions-route.test.tsx

<interfaces>
Renderer patterns to extend:
- Routes load through `window.agentWorkbench` and render sanitized DTOs only.
- `AppShell` already contains disabled later-phase navigation and a Sessions route.
- UI-SPEC is the authoritative copy/state/layout contract.
- Research resolved shadcn path: use existing CSS fallback unless `npm exec shadcn -- info` succeeds and official components require no unverified dependency install.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build split list/detail Data Sources route</name>
  <files>src/renderer/App.tsx, src/renderer/components/AppShell.tsx, src/renderer/components/DataSourceDetail.tsx, src/renderer/components/DataSourceList.tsx, src/renderer/components/DataSourcesLoadingSkeleton.tsx, src/renderer/components/SourceStatusBadge.tsx, src/renderer/routes/DataSourcesRoute.tsx, src/renderer/styles.css, tests/renderer/data-sources-route.test.tsx</files>
  <read_first>03-UI-SPEC full contract; 03-CONTEXT decisions D-01, D-02, D-03, D-04, D-05, D-07, D-08, D-10, D-13, D-15; 03-RESEARCH Resolved Planner Decisions; 03-PATTERNS Renderer Route analogs; src/renderer/routes/SessionsRoute.tsx; src/renderer/components/AppShell.tsx; src/renderer/styles.css</read_first>
  <behavior>
    - Test 1: `/data-sources` renders route title `Data Sources`, kicker `Local sources`, active Data Sources nav, and preserves Sessions navigation.
    - Test 2: source list rows show adapter/display name, root path, enabled/disabled, validation, scan/cache, diagnostic count, and watch support.
    - Test 3: detail panel owns adapter select, source name input, typed source root path input, enabled switch, validation summary, scan/cache summary, watch support, compact diagnostics, `Validate Source`, `Scan Source`, and `Rescan Source`.
    - Test 4: empty, loading, load-error, validation-failure, scan-failure, stale, unsupported, and unknown labels match UI-SPEC copy and are never rendered as zero/clean/passed.
    - Test 5: renderer does not read paths, expand paths, import main/adapters, branch on provider IDs, or render forbidden read-write controls.
  </behavior>
  <action>Implement `/data-sources` as the approved split list/detail settings route using existing CSS or already-validated official shadcn components only. First run `npm exec shadcn -- info`; if it fails, use the existing CSS fallback and do not install packages. If implementation attempts a new dependency such as `radix-ui`, stop and add a blocking human verification checkpoint because research marked it [ASSUMED]. Match the UI-SPEC state vocabulary and copy exactly where specified. Make `/data-sources` the default route if consistent with current App routing, while preserving `/sessions`. Use lucide icons already in the project. Keep sections un-nested and workbench-dense; no marketing hero, decorative cards, full Diagnostics page, native picker, delete/reset/clear-cache, session launch, approve/reject, terminal, PR, cleanup, or live watcher controls.</action>
  <verify>
    <automated>npm exec shadcn -- info</automated>
    <automated>npm run test:renderer -- tests/renderer/data-sources-route.test.tsx</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>The UI matches the approved split list/detail contract, supports keyboard row selection and reload/add/validate/scan workflows through the bridge, and preserves explicit unsupported/unknown/stale/failed diagnostics states.</acceptance_criteria>
  <done>UI-06 route is implemented around shared source state with no deferred UI ideas or forbidden mutation controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> preload bridge | User-entered source fields cross from renderer into typed preload calls. |
| renderer DTOs -> UI labels | Missing or unsupported evidence could be flattened into zero, clean, or passed labels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Information Disclosure | `DataSourcesRoute.tsx` | mitigate | Renderer must not expand paths, read files, import main/adapters, or show raw diagnostics beyond sanitized DTO fields. |
| T-03-08-02 | Spoofing | renderer source labels | mitigate | Drive labels from adapter descriptors and capability/source DTOs; no provider-specific branches or hardcoded Gemini behavior. |
| T-03-08-03 | Tampering | source action controls | mitigate | Renderer calls only named validate/scan/enable/update bridge methods; no source-root mutation, delete, reset, cache clear, or shell controls. |
| T-03-08-SC | Tampering | npm/pip/cargo installs | mitigate | Prefer existing CSS fallback. If implementation attempts any new install, especially `radix-ui`, add a blocking human verification checkpoint because slopcheck was unavailable. |
</threat_model>

<verification>
Plan-level checks:
- `npm exec shadcn -- info`
- `npm run test:renderer -- tests/renderer/data-sources-route.test.tsx`
- `npm run typecheck`
- `npm run test:boundaries`
</verification>

<success_criteria>
- UI-06 renderer route is complete and matches UI-SPEC.
- Data Sources UI consumes only preload view models.
- Deferred native picker, deep cache inspector, full Diagnostics page, full live watcher controls, Phase 6 dashboards/timeline/run-audit UI, and destructive/mutating controls are absent.
</success_criteria>

<source_coverage_audit>
GOAL: user can configure, validate, scan, and inspect source/cache diagnostics from Harnesses and Data Sources settings — COVERED.
REQ: UI-06 — COVERED.
RESEARCH: Data Sources split list/detail UI, shadcn validation gate, CSS fallback, no new dependencies without checkpoint — COVERED.
CONTEXT: D-01, D-02, D-03, D-04, D-05, D-07, D-08, D-10, D-13, D-15 — COVERED. Deferred native picker, deep cache inspector, full Diagnostics page, full live watcher controls, and broader Phase 6 UI are excluded.
</source_coverage_audit>

<output>
Create `.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-08-SUMMARY.md` when done.
</output>
