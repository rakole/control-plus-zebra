---
phase: 03-source-registry-scanner-cache-and-data-sources-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/main/core/adapter-contract/types.ts
  - src/main/core/security/index.ts
  - src/main/core/security/path-allowlist.ts
  - src/main/core/security/safe-filesystem.ts
  - src/main/adapters/fake-test/discovery.ts
  - src/main/adapters/fake-test/parse.ts
  - tests/main/core/safe-filesystem.test.ts
  - tests/adapters/fake-test/fake-adapter.contract.test.ts
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - "Adapters receive safe scoped filesystem helpers rather than direct unsafe traversal access per D-06 and DATA-02."
    - "Safe filesystem helpers allow configured-root access and indexed output-artifact access only."
    - "Unindexed output-artifact reads are rejected with structured diagnostics instead of being read as arbitrary files."
  artifacts:
    - path: "src/main/core/security/safe-filesystem.ts"
      provides: "Scoped read-only filesystem helpers for adapter context and indexed output artifacts"
      contains: "SafeFilesystem"
    - path: "src/main/core/security/path-allowlist.ts"
      provides: "Canonical source-root and artifact allowlist enforcement"
    - path: "tests/main/core/safe-filesystem.test.ts"
      provides: "Traversal, symlink escape, indexed artifact allowlist, and unindexed artifact rejection proof"
  key_links:
    - from: "src/main/core/adapter-contract/types.ts"
      to: "src/main/core/security/safe-filesystem.ts"
      via: "AdapterContext safe filesystem helper type"
      pattern: "safeFilesystem|SafeFilesystem"
    - from: "src/main/adapters/fake-test/discovery.ts"
      to: "src/main/core/security/safe-filesystem.ts"
      via: "AdapterContext helper calls, not direct fs/promises"
      pattern: "context\\.safeFilesystem"
    - from: "src/main/core/security/safe-filesystem.ts"
      to: "src/main/core/ingestion/raw-artifact-index.ts"
      via: "indexed output artifact allowlist contract consumed by scanner/cache plans"
      pattern: "indexed|artifact"
---

<objective>
Implement the scoped filesystem boundary and fake-adapter refactor required by DATA-02, including indexed output-artifact allowlisting and rejection diagnostics for unindexed artifact reads.

Purpose: Scanner and adapter work must run through shared read-only helpers, not adapter-owned filesystem traversal or arbitrary output-artifact reads.
Output: Safe filesystem modules, updated adapter context contract, fake adapter using scoped helpers, and focused security/contract tests.
</objective>

<execution_context>
@/Users/rhishi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rhishi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-CONTEXT.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-RESEARCH.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-PATTERNS.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-01-SUMMARY.md
@src/main/core/adapter-contract/types.ts
@src/main/adapters/fake-test/discovery.ts
@src/main/adapters/fake-test/parse.ts
@tests/contract/run-adapter-contract.ts

<interfaces>
Contracts from 03-01 and existing code:
- `SourceRegistry` records provide configured source roots and enabled state.
- `AdapterContext` currently has `projectDir` and `platform`; extend it with scoped helpers while preserving existing test ergonomics.
- Fake adapter lifecycle still uses `validateSourceRoot`, `discoverSources`, `discoverArtifacts`, `parseArtifact`, and `normalize`.
- DATA-02 requires helpers scoped to configured source roots and indexed output artifacts; both parts must be covered.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add safe filesystem helpers and indexed artifact allowlist</name>
  <files>src/main/core/adapter-contract/types.ts, src/main/core/security/index.ts, src/main/core/security/path-allowlist.ts, src/main/core/security/safe-filesystem.ts, tests/main/core/safe-filesystem.test.ts</files>
  <read_first>03-CONTEXT decisions D-06 and D-12; 03-RESEARCH Phase Requirements DATA-02 and Safe Filesystem Helper Sketch; 03-PATTERNS Safe Filesystem analog; src/main/core/adapter-contract/types.ts</read_first>
  <behavior>
    - Test 1: helpers allow stat/read/list only for paths that canonicalize inside enabled configured source roots.
    - Test 2: `../` traversal and symlink escape attempts are rejected with typed errors or diagnostics before adapter parsing.
    - Test 3: helpers allow output-artifact reads only when the artifact identity/path/native ref is present in the raw artifact index allowlist supplied by scanner/index code.
    - Test 4: unindexed output-artifact read attempts return a structured rejection diagnostic with source/artifact identity where available and do not read the file.
    - Test 5: disabled or unknown source state rejects reads as unsupported/unknown, never as empty or clean.
  </behavior>
  <action>Extend `AdapterContext` with narrow read-only safe filesystem helpers while preserving existing `projectDir` and `platform` fields. Implement canonical realpath enforcement for configured roots plus a separate indexed output-artifact allowlist hook that can be populated by the raw artifact index in later scanner plans. The helper API must support source-root stat/read/list and output-artifact read by indexed artifact identity; it must reject path traversal, symlink escape, disabled sources, and unindexed output-artifact reads with diagnostics or typed errors. Do not expose raw `fs`, shell execution, cache writes, watcher lifecycle, or broad path reads to adapters.</action>
  <verify>
    <automated>npm test -- tests/main/core/safe-filesystem.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Safe filesystem helpers enforce configured-root scope and indexed output-artifact allowlisting, and unindexed output-artifact reads are rejected with visible diagnostics.</acceptance_criteria>
  <done>DATA-02 helper contract is complete for both configured roots and indexed output artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor fake adapter reads through scoped helpers</name>
  <files>src/main/adapters/fake-test/discovery.ts, src/main/adapters/fake-test/parse.ts, tests/adapters/fake-test/fake-adapter.contract.test.ts, tests/main/core/safe-filesystem.test.ts</files>
  <read_first>03-CONTEXT decision D-12; 03-PATTERNS current direct stat/read gaps; src/main/adapters/fake-test/discovery.ts; src/main/adapters/fake-test/parse.ts; tests/contract/run-adapter-contract.ts</read_first>
  <behavior>
    - Test 1: fake adapter validation, artifact discovery, and parsing use `AdapterContext.safeFilesystem` and no longer import `node:fs/promises` for source-root or output-artifact reads.
    - Test 2: fake adapter contract tests still pass with scoped helpers.
    - Test 3: fake output-artifact loading, if present, succeeds only for indexed artifacts and reports diagnostics for unindexed artifacts.
  </behavior>
  <action>Refactor fake adapter source validation, artifact metadata, fixture parsing, and any output-artifact loading path to call context helpers instead of direct `stat`, `readFile`, or unrestricted path traversal. Keep adapter code evidence-only: diagnostics are allowed, but final verification states, run-audit classifications, attention reasons, watcher lifecycle, and cache writes remain forbidden. Preserve fake adapter usefulness as the Phase 3 proof source without hardcoding Phase 3 shared logic to fake-specific paths.</action>
  <verify>
    <automated>npm test -- tests/adapters/fake-test/fake-adapter.contract.test.ts tests/main/core/safe-filesystem.test.ts</automated>
    <automated>npm run test:boundaries</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Fake adapter lifecycle still works through scoped helpers and attempts to bypass indexed artifact access fail with diagnostics.</acceptance_criteria>
  <done>DATA-02 is satisfied for the fake proof adapter and future scanner work can build safe adapter contexts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared scanner -> adapter | Adapter receives scoped helpers and source records but must not gain arbitrary filesystem access. |
| adapter output artifact load -> filesystem | Artifact reads may reference paths outside roots unless indexed and allowlisted first. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | `safe-filesystem.ts` | mitigate | Canonicalize real paths and enforce configured source-root allowlists before stat/read/list operations. |
| T-03-02-02 | Tampering | output-artifact reads | mitigate | Permit output-artifact reads only through indexed artifact identity and reject unindexed reads with diagnostics. |
| T-03-02-03 | Elevation of Privilege | `AdapterContext` | mitigate | Expose narrow read-only helpers only; adapters do not receive raw `fs`, shell execution, cache write, or watcher lifecycle authority. |
| T-03-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package install is planned. If implementation attempts any install, stop and add a blocking human verification checkpoint because slopcheck was unavailable. |
</threat_model>

<verification>
Plan-level checks:
- `npm test -- tests/main/core/safe-filesystem.test.ts tests/adapters/fake-test/fake-adapter.contract.test.ts`
- `npm run test:boundaries`
- `npm run typecheck`
</verification>

<success_criteria>
- DATA-02 has behavior tests for root scoping, traversal/symlink rejection, indexed output-artifact allowlisting, and unindexed output-artifact diagnostics.
- Fake adapter remains usable as the Phase 3 proof source per D-12.
- Adapters still emit evidence and diagnostics only.
</success_criteria>

<source_coverage_audit>
GOAL: configure and validate source roots without unsafe adapter filesystem behavior — COVERED by Tasks 1-2.
REQ: DATA-02 — COVERED by Tasks 1-2, including indexed output artifacts and unindexed rejection diagnostics.
RESEARCH: safe filesystem, fake adapter refactor, no new dependencies — COVERED.
CONTEXT: D-06 and D-12 — COVERED. Deferred live watcher controls and broad filesystem APIs are excluded.
</source_coverage_audit>

<output>
Create `.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-02-SUMMARY.md` when done.
</output>
