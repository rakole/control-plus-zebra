---
phase: 03-source-registry-scanner-cache-and-data-sources-ui
plan: 06
type: execute
wave: 5
depends_on:
  - 03-05
files_modified:
  - src/main/app/session-view-model-service.ts
  - tests/main/ipc/session-view-model-service.test.ts
autonomous: true
requirements:
  - DATA-03
  - DATA-08
must_haves:
  truths:
    - "Sessions loaded for existing renderer flows come from the source/scanner/cache path rather than hardcoded fake fixture shortcuts per D-12."
    - "Existing Sessions IPC and renderer DTOs remain sanitized and bridge-backed."
    - "Absence of configured or scanned sources returns an honest empty or unknown state instead of fake fixture success."
  artifacts:
    - path: "src/main/app/session-view-model-service.ts"
      provides: "Existing session view models backed by source/scanner/cache data"
    - path: "tests/main/ipc/session-view-model-service.test.ts"
      provides: "Session service migration proof"
  key_links:
    - from: "src/main/app/session-view-model-service.ts"
      to: "src/main/core/cache/file-backed-cache-store.ts"
      via: "read normalized cached sessions for existing Sessions route"
      pattern: "FileBackedCacheStore|cache"
    - from: "src/main/app/session-view-model-service.ts"
      to: "src/main/core/ingestion/scanner.ts"
      via: "configured fake source scan/cache path"
      pattern: "scanner|scan"
---

<objective>
Migrate the existing Sessions view-model service away from direct fake fixture loading and onto the source/scanner/cache path created in prior plans.

Purpose: Phase 3 should keep the fake adapter as a proof source without leaving the main session read path hardcoded to fake fixtures.
Output: Updated session view-model service and focused service tests.
</objective>

<execution_context>
@/Users/rhishi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rhishi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-CONTEXT.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-RESEARCH.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-PATTERNS.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-05-SUMMARY.md
@src/main/app/session-view-model-service.ts
@src/main/ipc/view-models.ts
@tests/main/ipc/ipc-handlers.test.ts
@tests/renderer/sessions-route.test.tsx

<interfaces>
Contracts from prior plans to use directly:
- `SourceRegistry`, scanner, raw artifact index, normalization validator, session merger, and file-backed cache now own the source-driven data path.
- Session view-model DTO schemas in `src/main/ipc/view-models.ts` remain the renderer boundary.
- Fake adapter remains the first proof source per D-12 but must not shape shared service naming.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route existing session view models through source cache path</name>
  <files>src/main/app/session-view-model-service.ts, tests/main/ipc/session-view-model-service.test.ts</files>
  <read_first>03-CONTEXT decision D-12; 02-02 and 02-04 SUMMARYs; 03-05 SUMMARY; src/main/app/session-view-model-service.ts; src/main/ipc/view-models.ts</read_first>
  <behavior>
    - Test 1: session list/preview service loads normalized sessions from the registry/scanner/cache path when a fake source is configured and scanned.
    - Test 2: service keeps sanitized renderer DTOs and does not expose raw paths, raw events, adapter-private objects, final verification state, run-audit status, or attention reasons.
    - Test 3: absence of configured/scanned sources returns an honest empty or unknown state rather than hardcoded fake fixture data.
  </behavior>
  <action>Refactor `session-view-model-service.ts` so the Sessions route can consume normalized data produced by the source/scanner/cache path instead of directly hardcoding the fake fixture lifecycle. Keep the fake adapter usable as the first proof source per D-12, but make the service harness-neutral and injectable for tests. Preserve all existing sanitized DTO schemas and Phase 2 renderer behavior; this task must not add Phase 6 dashboard/timeline/run-audit UI scope.</action>
  <verify>
    <automated>npm test -- tests/main/ipc/session-view-model-service.test.ts</automated>
    <automated>npm run test -- tests/main/ipc/ipc-handlers.test.ts tests/renderer/sessions-route.test.tsx</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Existing Sessions IPC/renderer tests continue to pass while data comes through source/cache services, and no hardcoded adapter-specific UI behavior is added.</acceptance_criteria>
  <done>Existing session viewing path is ready for Phase 3 source-driven data without breaking Phase 2 bridge contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cache files -> main-process view models | Cache files may be missing, corrupt, stale, or tampered with. |
| main service -> renderer DTOs | Session data must remain sanitized and harness-neutral. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Information Disclosure | `session-view-model-service.ts` | mitigate | Continue returning sanitized DTOs only; never return raw artifact paths, raw events, stack traces, or adapter-private object names. |
| T-03-06-02 | Spoofing | session source identity | mitigate | Load sessions from adapter/source-aware cache records and preserve source IDs in DTO derivation. |
| T-03-06-03 | Tampering | missing source/cache data | mitigate | Treat absence of configured/scanned sources as empty/unknown, not fake fixture success. |
| T-03-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package install is planned. If implementation attempts any install, stop and add a blocking human verification checkpoint because slopcheck was unavailable. |
</threat_model>

<verification>
Plan-level checks:
- `npm test -- tests/main/ipc/session-view-model-service.test.ts`
- `npm run test -- tests/main/ipc/ipc-handlers.test.ts tests/renderer/sessions-route.test.tsx`
- `npm run typecheck`
</verification>

<success_criteria>
- Existing Sessions route behavior remains bridge-backed and sanitized.
- Source/cache path supplies normalized sessions; hardcoded fake fixture lifecycle is removed from the service.
- No Phase 6 dashboard, detail timeline, verification, or run-audit scope is introduced.
</success_criteria>

<source_coverage_audit>
GOAL: shared ingestion and cache pipeline feeds app session views — COVERED.
REQ: DATA-03 and DATA-08 — COVERED through consuming scanner/cache outputs.
RESEARCH: migrate hardcoded fake service path, preserve sanitized IPC DTOs — COVERED.
CONTEXT: D-06 and D-12 — COVERED. Deferred Phase 6 UI and audit conclusions are excluded.
</source_coverage_audit>

<output>
Create `.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-06-SUMMARY.md` when done.
</output>
