---
phase: 03-source-registry-scanner-cache-and-data-sources-ui
plan: 06
type: execute
wave: 5
depends_on:
  - 03-05
files_modified:
  - src/main/core/ingestion/scanner.ts
  - src/main/core/registry/source-registry.ts
  - src/main/core/registry/source-registry-store.ts
  - tests/main/core/scanner-cache.test.ts
  - tests/main/core/source-registry.test.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-08
must_haves:
  truths:
    - "Scanner writes validated normalized scan results into the file-backed cache from 03-05 without letting adapters write cache directly."
    - "Source/cache summaries expose never scanned, scanning, scan failed, scanned with diagnostics, cached, stale, unsupported, and unknown truth per D-07."
    - "Parser, source, cache, and normalization diagnostics remain visible enough for the Data Sources detail panel per D-08."
  artifacts:
    - path: "src/main/core/ingestion/scanner.ts"
      provides: "Scanner-to-cache integration"
    - path: "src/main/core/registry/source-registry.ts"
      provides: "Source scan/cache summary state"
    - path: "tests/main/core/scanner-cache.test.ts"
      provides: "Scanner-to-cache integration proof"
    - path: "tests/main/core/source-registry.test.ts"
      provides: "Source summary persistence proof"
  key_links:
    - from: "src/main/core/ingestion/scanner.ts"
      to: "src/main/core/cache/file-backed-cache-store.ts"
      via: "validated normalized scan results write cache"
      pattern: "cache"
    - from: "src/main/core/registry/source-registry.ts"
      to: "src/main/core/ingestion/scanner.ts"
      via: "scan/cache summaries updated from scanner results"
      pattern: "scan.*cache"
---

<objective>
Integrate the scanner and source registry with the cache foundation from 03-05.

Purpose: Data Sources UI needs honest scan/cache summaries derived from shared scanner/cache state, not adapter-owned cache writes or flattened success labels.
Output: Scanner cache integration, source summary updates, and focused integration tests.
</objective>

<execution_context>
@/Users/rhishi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rhishi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-CONTEXT.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-RESEARCH.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-PATTERNS.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-03-SUMMARY.md
@.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-05-SUMMARY.md
@src/main/core/ingestion/scanner.ts
@src/main/core/registry/source-registry.ts

<interfaces>
Contracts from prior plans to use directly:
- `NormalizationValidator` only returns valid normalized fragments for merge/cache writes.
- `FileBackedCacheStore` from 03-05 persists normalized scan payloads and explicit cache metadata.
- `SourceRegistry` persists validation, scan, cache, diagnostics, and enabled/disabled summaries.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate cache with scanner and source summaries</name>
  <files>src/main/core/ingestion/scanner.ts, src/main/core/registry/source-registry.ts, src/main/core/registry/source-registry-store.ts, tests/main/core/scanner-cache.test.ts, tests/main/core/source-registry.test.ts</files>
  <read_first>03-CONTEXT decisions D-07, D-08, D-09, D-11, D-12; 03-RESEARCH Planner Test Map DATA-08; 03-05 SUMMARY; src/main/core/ingestion/scanner.ts; src/main/core/registry/source-registry.ts</read_first>
  <behavior>
    - Test 1: successful scan writes normalized cache and updates source scan/cache summary to cached.
    - Test 2: parser/cache/normalization diagnostics update source summary to scanned with diagnostics or scan failed as appropriate.
    - Test 3: changed index/cache inputs mark source cache state stale and preserve concise reason diagnostics.
    - Test 4: unsupported or unknown adapter/cache capability remains unsupported/unknown, never zero or clean.
  </behavior>
  <action>Wire scanner results from 03-03 into the file-backed cache store and registry summaries from 03-01/03-05. Update source records with scan/cache state transitions for never scanned, scanning, scan failed, scanned with diagnostics, cached, stale, unsupported, and unknown per D-07. Preserve source, parser, cache, and normalization diagnostics enough for the Data Sources detail panel per D-08, but do not build a deep cache inspector or full Diagnostics route. Scanner/cache integration must continue validating normalized output before writes and must not let adapters write cache directly.</action>
  <verify>
    <automated>npm test -- tests/main/core/scanner-cache.test.ts tests/main/core/source-registry.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Scanner writes cache only after validation, source summaries reflect cache/stale/failed/diagnostics states, and unsupported/unknown cache truth is explicit.</acceptance_criteria>
  <done>DATA-08 is connected to the scanner and source registry with honest cache state summaries for Data Sources UI consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner output -> cache store | Only validated normalized data should be written to cache. |
| scanner/cache summaries -> source registry | Source state must preserve failed, stale, diagnostics-bearing, unsupported, and unknown truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | `scanner.ts` | mitigate | Write cache only after normalization validation succeeds; convert invalid output to diagnostics. |
| T-03-06-02 | Repudiation | source cache summaries | mitigate | Persist scan/cache state, diagnostics, and timestamps so stale/failed/unknown states remain inspectable. |
| T-03-06-03 | Spoofing | adapter/cache capability state | mitigate | Preserve unsupported/unknown capability truth from adapters and cache state rather than deriving clean labels from counts. |
| T-03-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package install is planned. If implementation attempts any install, stop and add a blocking human verification checkpoint because slopcheck was unavailable. |
</threat_model>

<verification>
Plan-level checks:
- `npm test -- tests/main/core/scanner-cache.test.ts tests/main/core/source-registry.test.ts`
- `npm run typecheck`
- `npm run test:boundaries`
</verification>

<success_criteria>
- Scanner writes validated normalized data to the file-backed cache.
- Source/cache state preserves cached, stale, failed, diagnostics-bearing, unsupported, and unknown truth.
- Adapters still cannot write cache directly or own final source/cache conclusions.
</success_criteria>

<source_coverage_audit>
GOAL: run shared ingestion through a safe cache pipeline with visible source/cache diagnostics — COVERED.
REQ: DATA-01 and DATA-08 — COVERED for source summary/cache integration.
RESEARCH: scanner/cache integration, source summaries, stale/unknown truth, no deep cache inspector — COVERED.
CONTEXT: D-07, D-08, D-09, D-11, D-12 — COVERED. Deferred SQLite and deep cache inspector are excluded.
</source_coverage_audit>

<output>
Create `.planning/phases/03-source-registry-scanner-cache-and-data-sources-ui/03-06-SUMMARY.md` when done.
</output>
